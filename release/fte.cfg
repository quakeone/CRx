// AfterQuake config file

setinfo * ""
setinfo "pmodel" "33168"

p2 setinfo * ""
p2 setinfo "name" "Wart"
p2 setinfo "aim" "0.93"

p3 setinfo * ""

p4 setinfo * ""

serverinfo * ""
serverinfo "map" "start"

localinfo * ""
localinfo "d_skill" "1"
localinfo "team0" "t0"
localinfo "team1" "t1"
localinfo "team2" "t2"
localinfo "team3" "t3"
localinfo "team4" "red"
localinfo "team5" "t5"
localinfo "team6" "t6"
localinfo "team7" "t7"
localinfo "team8" "t8"
localinfo "team9" "t9"
localinfo "team10" "t10"
localinfo "team11" "t11"
localinfo "team12" "t12"
localinfo "team13" "blue"
localinfo "team14" "urk"
localinfo "team15" "wut"

// autocvars
set hud_matchtimer "1"

// Custom variables
set that "to gl_nearest"
set nextserver ""
set sv_readonlyworld "1"
set g_gametype ""
set dp_workarounds_force "0"
set dp_workarounds_allow "1"
set cl_cursorbias "4"
set cl_cursorsize "32"
set menu_font ""
set menu_font_win ""
set r_menutint_inverse ""
set r_glsl_pcf "0"
set gl_ext_GL_ARB_ES3_2_compatibility "1"
set gl_ext_GL_KHR_texture_compression_astc_ldr "1"
set gl_ext_GL_KHR_texture_compression_astc_hdr "1"
set gl_ext_GL_OES_texture_compression_astc "1"
set gl_ext_GL_ANGLE_texture_compression_dxt5 "1"
set gl_ext_GL_ANGLE_texture_compression_dxt3 "1"
set gl_ext_GL_EXT_texture_compression_dxt1 "1"
set gl_ext_GL_EXT_texture_compression_s3tc "1"
set gl_ext_GL_ARB_framebuffer_sRGB "1"
set gl_ext_GL_ARB_seamless_cube_map "1"
set gl_ext_GL_ARB_texture_env_dot3 "1"
set gl_ext_GL_ARB_texture_env_combine "1"
set gl_ext_GL_NV_texture_env_combine4 "1"
set gl_ext_GL_EXT_texture_env_add "1"
set gl_ext_GL_EXT_texture_env_combine "1"
set gl_ext_GL_EXT_compiled_vertex_array "1"
set gl_ext_GL_EXT_texture_object "1"
set gl_ext_GL_ATI_pn_triangles "1"
set gl_ext_GL_EXT_texture_compression_astc_decode_mode "1"
set gl_ext_GL_ARB_depth_clamp "1"
set gl_ext_GL_EXT_stencil_two_side "1"
set gl_ext_GL_EXT_stencil_wrap "1"
set gl_ext_GL_ARB_multitexture "1"
set gl_ext_GL_EXT_texture_filter_anisotropic "1"
set gl_ext_GL_EXT_packed_depth_stencil "1"
set gl_ext_WGL_ARB_create_context_profile "1"
set gl_ext_WGL_ARB_create_context_robustness "1"
seta r_part_classic_square "1"
seta vid_refreshrate "60"
seta r_deluxmapping "2"
seta gl_blacklist_debug_glsl "0"
seta cross_color "0 255 0"
seta vgui_color "255 170 0"
seta con_color "255 170 0"
seta v_bobup "0.5"
seta v_bob "0"
seta v_bobcycle "0.8"
seta fcs_autoreload "0"
seta fcs_fillweapons "0"
seta fcs_maxmoney "16000"
seta fcs_penalty_kill "-1500"
seta fcs_penalty_pain "-150"
seta fcs_reward_kill "300"
seta fcs_nopickups "0"
seta fcs_swapteams "0"
seta fcs_knifeonly "0"
seta mp_roundtime "5"
seta mp_c4timer "45"
seta mp_freezetime "6"
seta mp_buytime "90"
seta mp_startmoney "800"
seta r_imageexensions "dds ktx tga png jpg pcx" //The list of image file extensions which fte should attempt to load.
seta v_viewmodel_quake "1"
set gl_blacklist_invariant "0"
set cl_cdkey ""
set sb_showname "1"
set sb_showmaxplayers "0"
set sb_shownumplayers "1"
set v_gammainverted "1"

// Text editor
pr_debugger "0"         //When enabled, QC errors and debug events will enable step-by-step tracing.
edit_addcr ""           //make sure that each line ends with a \r (on save). Empty will be assumed to be 1 on windows and 0 otherwise.
edit_stripcr "1"        //remove \r from eols (on load)

// GLTF Models
set mod_gltf_ignoretechniques "1"       //Ignore the gltf1 model-specific glsl. This is enabled by default because it just doesn't work very well.
set mod_gltf_privatematerials "1"       //Add the model path to material names, to isolate them between different models.
set mod_gltf_fixbuggyanims "1"  //Work around buggy exporters by merging animations that affect only a single bone.
set mod_gltf_scale "30" //This defines the number of units per metre, in order to correctly load standard-scale gltf models.

// plugins
plug_loaddefault "1"    //0: Load plugins only via explicit plug_load commands
                        //1: Load built-in plugins and those selected via the package manager
                        //2: Scan for misc plugins, loading all that can be found, but not built-ins.
                        //3: Scan for plugins, and then load any built-ins
plug_sbar "3"           //Controls whether plugins are allowed to draw the hud, rather than the engine (when allowed by csqc). This is typically used to permit the ezhud plugin without needing to bother unloading it.
                        //=0: never use hud plugins.
                        //&1: Use hud plugins in deathmatch.
                        //&2: Use hud plugins in singleplayer/coop.
                        //=3: Always use hud plugins (when loaded).

// Player Ignoring
ignore_opponents "0"    //0: Don't ignore chat from enemies.
                        //1: Always ignore chat from opponents (note: can also ignore f_ruleset checks).
                        //2: Ignore chat from opponents only during a match (requires servers that actually reports match state).
                        //
ignore_mode "0"
ignore_qizmo_spec "0"
ignore_spec "0"         //0: Never ignore spectators.
                        //1: Ignore spectators only when playing.
                        //2: Always ignore spectators even when spectating.
ignore_flood "0"        //Provides a way to reduce inbound spam from flooding out your chat (dupe messages are ignored).
                        //0: No inbound flood protection.
                        //1: Duplicate non-team messages will be filtered.
                        //2: ALL duplicate messages will be filtered
ignore_flood_duration "4"       //Time limit for inbound messages to be considered duplicates.

// statusbar
set hud_miniscores_show "1"
set hud_tracking_show "1"

// Cheats
seta cl_fullpitch "0"   //When set, attempts to unlimit the default view pitch. Note that some servers will screw over your angles if you use this, resulting in terrible gameplay, while some may merely clamp your angle serverside. This is also considered a cheat in quakeworld, ^1so this will not function there^7. For the equivelent in quakeworld, use serverinfo minpitch+maxpitch instead, which applies to all players fairly.

// Item effects
seta r_drawflame "0"    //Set to -1 to disable ALL static entities. Set to 0 to disable only wall torches and standing flame. Set to 1 for everything drawn as normal.
cl_nolerp_netquake "0"  //Disables interpolation when connected to an NQ server. Does affect players, even the local player. You probably don't want to set this.
seta cl_nolerp "0"      //Disables interpolation. If set, missiles/monsters will be show exactly what was last received, which will be jerky. Does not affect players. A value of 2 means 'interpolate only in single-player/coop'.
seta cl_deadbodyfilter "0"
seta cl_gibfilter "0"
v_powerupshell "0"
r_powerupglow "1"
cl_r2g "0"              //Uses progs/grenade.mdl instead of progs/missile.mdl when 1.
r_lightflicker "1"
r_rocketlight "1"
r_torch "0"
cl_staticsounds "1"
seta gl_simpleitems "0"
cl_model_bobbing "0"

// client input controls
m_side "0.8"
m_forward "1"
m_yaw "0.022"
m_pitch "0.022"
sensitivity "2.6"
lookstrafe "0"
lookspring "0"
cl_anglespeedkey "1.025"
cl_pitchspeed "150"
cl_yawspeed "10"
seta cl_movespeedkey "999"
seta cl_sidespeed "500"
seta cl_backspeed "320" //The base speed that you move backwards at. If empty, uses the value of cl_forwardspeed instead.
seta cl_forwardspeed "320"
cl_upspeed "320"

// Demo playback
cl_demospeed "-0.1"

// CSQC progs control
dpcompat_csqcinputeventtypes "999999"   //Specifies the first csqc input event that the mod does not recognise. This should never have been a thing, but some mods are simply too buggy.
cl_csqc_nodeprecate "0" //When set, disables deprecation warnings.
pr_csqc_coreonerror "1"
cl_nocsqc "0"
cl_csqcdebug "0"
pr_csqc_maxedicts "65536"
pr_csqc_memsize "-1"
pr_csqc_formenus "0"

// Menu progs control
pr_menu_memsize "64m"
pr_menu_coreonerror "1"
forceqmenu "0"

// Teamplay Variables
tp_name_someplace "someplace"
tp_soundtrigger "~"
tp_loadlocs "1"
tp_forceTriggers "0"
tp_autostatus ""
cl_triggers "1"
cl_parseFunChars "1"
cl_parseSay "1"
cl_fakename ""
loc_name_suit "suit"
loc_name_ring "ring"
loc_name_pent "pent"
loc_name_quad "quad"
loc_name_ra "ra"
loc_name_ya "ya"
loc_name_ga "ga"
loc_name_lg "lg"
loc_name_rl "rl"
loc_name_gl "gl"
loc_name_sng "sng"
loc_name_ng "ng"
loc_name_ssg "ssg"
loc_name_separator "-"
tp_name_eyes "eyes"
tp_name_teammate ""
tp_name_enemy "enemy"
tp_name_separator "/"
tp_name_pented "pented"
tp_name_quaded "quaded"
tp_weapon_order "78654321"
tp_name_weapon "weapon"
tp_name_armor "armor"
tp_name_armortype_ra "r"
tp_name_armortype_ya "y"
tp_name_armortype_ga "g"
tp_name_status_blue "$B"
tp_name_status_yellow "$Y"
tp_name_status_green "$G"
tp_name_status_red "$R"
tp_name_rune_4 "regeneration rune"
tp_name_rune_3 "haste rune"
tp_name_rune_2 "strength rune"
tp_name_rune_1 "resistance rune"
tp_name_sentry "sentry gun"
tp_name_disp "dispenser"
tp_need_shells "10"
tp_need_nails "40"
tp_need_cells "20"
tp_need_rockets "5"
tp_need_rl "1"
tp_need_weapon "35687"
tp_need_health "50"
tp_need_ga "50"
tp_need_ya "50"
tp_need_ra "50"
tp_name_at "at"
tp_name_nothing "nothing"
tp_name_flag "flag"
tp_name_backpack "pack"
tp_name_health "health"
tp_name_mh "mega"
tp_name_cells "cells"
tp_name_rockets "rockets"
tp_name_nails "nails"
tp_name_shells "shells"
tp_name_suit "suit"
tp_name_ring "ring"
tp_name_pent "pent"
tp_name_quad "quad"
tp_name_ga "ga"
tp_name_ya "ya"
tp_name_ra "ra"
tp_name_lg "lg"
tp_name_rl "rl"
tp_name_gl "gl"
tp_name_sng "sng"
tp_name_ng "ng"
tp_name_ssg "ssg"
tp_name_sg "sg"
tp_name_axe "axe"
tp_name_none ""
teamcolor "blue"
enemycolor ""
cl_enemyskin "0"
cl_teamskin "0"

// Spectator Tracking
cl_chasecam "1"
cl_hightrack "0"        //Obsolete. If you want hightrack, use '[cl_]autotrack high' instead.
cl_autotrack_team ""    //Specifies a team name that should be auto-tracked (players on other teams will not be candidates for autotracking). Accepts * and ? wildcards for awkward chars.
cl_autotrack "auto"     //Specifies the default tracking mode at the start of the map. Use the 'autotrack' command to reset/apply an auto-tracking mode without changing the default.
                        //Valid values are: high, ^hkiller^h, mod, user. Other values are treated as weighting scripts for mvd playback, where available.

// Client side prediction
cl_solid_players "1"    //Consider other players as solid for player prediction.
cl_predict_players_nudge "0.02" //An extra nudge of time, to cover video latency.
cl_predict_players_latency "1.0"        //Push the player back according to your latency, to give a smooth consistent simulation of the server.
cl_predict_players_frac "0.9"   //How much of other players to predict. Values less than 1 will help minimize overruns.
cl_predict_players "1"  //Clear this cvar to see ents exactly how they are on the server.
cl_lerp_driftfrac "0"   //Proportion of the latest time vs the older time to favour drifting towards.
cl_lerp_driftbias "0"   //Additional bias, can be set to a negative value to hold interpolation in the past.
cl_lerp_smooth "1"      //If 2, will act as 1 when playing demos/singleplayer and otherwise act as if set to 0 (ie: deathmatch).
                        //If 1, interpolation will run in the past, resulting in really smooth movement at the cost of latency (even on bunchy german ISDNs).
                        //If 0, interpolation will be based upon packet arrival times and may judder due to packet loss.
cl_predict_timenudge "0"        //A debug feature. You should normally leave this as 0. Nudges local player prediction into the future if positive (resulting in extrapolation), or into the past if negative (resulting in laggy interpolation). Value is in seconds, so small decimals are required. This cvar applies even if input frames are tied to video frames.
cl_predict_extrapolate ""       //If 1, enables prediction based upon partial input frames which can change over time resulting in a swimmy feel but does not need to interpolate. If 0, prediction will stay in the past and thus use only completed frames. Interpolation will then be used to smooth movement.
                                //This cvar only applies when video and input frames are independant (ie: cl_netfps is set).
cl_nopred "0"           //Disables clientside movement prediction.
pushlatency "-999"

// Temporary entity control
cl_part_density_fade "1024"     //Specifies the distance over which ssqc pointparticles density fades from all to none. If this is set to 0 then particles will spawn at their normal density regardless of location on the map.
cl_part_density_fade_start "1024"       //Specifies the distance at which ssqc's pointparticles will start to get less dense.
gl_shaftlight "0.8"
cl_legacystains "1"     //WARNING: this cvar will default to 0 and later removed at some point
r_rocketlight_colour "2.0 1.0 0.25 200"
r_brightlight_colour "2.0 1.0 0.5 400"
r_dimlight_colour "2.0 1.0 0.5 200"
r_muzzleflash_fade "1.5 0.75 0.375 1000"
r_muzzleflash_colour "1.5 1.3 1.0 200"
r_explosionlight_fade "0.784 0.92 0.48"
r_explosionlight_colour "4.0 2.0 0.5"
r_explosionlight "1"
cl_beam_trace "0"
cl_truelightning "1"
seta cl_expsprite "0"   //Display a central sprite in explosion effects. QuakeWorld typically does so, NQ mods should not (which is problematic when played with the qw protocol).

// client networking options
r_viewpreselgun "0"     //HACK: Display the preselected weaponmodel, instead of the current weaponmodel.
cl_weaponforgetorder "0"        //The 'weapon' command will lock in its weapon choice, instead of choosing a different weapon between select+fire.
cl_weaponpreselect "0"  //HACK: Controls the interaction between the ^aweapon^a and ^a+attack^a commands (does not affect ^aimpulse^a).
                        //0: weapon switch happens instantly
                        //1: weapon switch happens on next attack
                        //2: instant only when already firing, otherwise delayed
                        //3: delay until new attack only in deathmatch 1
                        //4: delay until any attack only in deathmatch 1
cl_weaponhide_preference "2 1"  //The weapon you would like to try to switch to when cl_weaponhide is active
cl_weaponhide "0"       //HACK: Attempt to switch weapon to another in order to cheat your killer out of any possible weapon upgrades.
                        //0: original behaviour
                        //1: switch away when +attack/+fire is released
                        //2: switch away only in deathmatch
                        //
in_forceseat "0"        //Overrides the device identifiers to control a specific client from any device. This can be used for debugging mods, where you only have one keyboard/mouse.
cl_instantrotate "1"
cl_prydoncursor ""
cl_smartjump "1"
cl_sendchatstate "1"    //Announce your chat state to the server in a privacy-violating kind of way. This allows other players to see your afk/at-console status.
cl_movement "1"         //Specifies whether to send movement sequence info over DPP7 protocols (other protocols are unaffected). Unlike cl_nopred, this can result in different serverside behaviour.
cl_iDrive "1"           //Effectively releases movement keys when the opposing key is pressed. This avoids dead-time when both keys are pressed. This can be emulated with various scripts, but that's messy.
cl_run "0"              //Enables autorun, inverting the state of the +speed key.
cl_sparemsec "10"
cl_netfps "999"
cl_queueimpulses "0"
cl_c2spps "0"
cl_c2sImpulseBackup "3"
cl_c2sdupe "0"
prox_inmenu "0"
cl_nodelta "0"
in_xflip "0"
cl_fastaccel "1"        //Begin moving at full speed instantly, instead of waiting a frame or so.

// Authentication
ruleset "none"          //Known rulesets are:
                        //none: no explicit rules, all 'minor cheats' are allowed.
                        //strict: equivelent to the smackdown ruleset. Note that this will block certain graphical enhancements too.
allow_f_cmdline "0"
allow_f_system "0"
allow_f_scripts "1"
allow_f_ruleset "1"
allow_f_skins "1"
allow_f_fakeshaft "1"
allow_f_modified "1"
allow_f_server "1"
allow_f_version "1"

// Status bar settings
cl_sbaralpha "0"        //Specifies the transparency of the status bar. Only Takes effect when cl_sbar is set to 2.
sbar_teamstatus "2"     //Display the last team say from each of your team members just above the sbar area.

// Scoreboard settings
scr_scoreboard_titleseperator "1"
scr_scoreboard_teamsort "0"     //On the scoreboard, sort players by their team BEFORE their personal score.
scr_scoreboard_teamscores "1"   //Makes +showscores act as +showteamscores. Because reasons.
scr_scoreboard_backgroundalpha "0.5"    //Further multiplier for the background alphas.
scr_scoreboard_fillalpha "0.8"  //Transparency amount for newstyle scoreboard.
scr_scoreboard_showflags "2"    //Display flag caps+touches on the scoreboard, where our fragfile.dat supports them.
                                //0: off
                                //1: on
                                //2: on only if someone appears to have interacted with a flag.
scr_scoreboard_showfrags "0"    //Display kills+deaths+teamkills, as determined by fragfile.dat-based conprint parsing. These may be inaccurate if you join mid-game.
scr_scoreboard_newstyle "1"     //Display team colours and stuff in a style popularised by Electro. Looks more modern, but might not quite fit classic huds.
scr_scoreboard_forcecolors "0"  //Makes the scoreboard colours obey enemycolor/teamcolor rules.
scr_scoreboard_drawtitle "1"    //Wastes screen space when looking at the scoreboard.

// WASAPI audio output
wasapi_buffersize "0.01"
wasapi_exclusive "0"    //When set, attempts to take exclusive control of the output device, to the detriment of other programs (causing errors or even crashes in them).
                        //Exclusive mode leaves the game free to change the hardware's playback details, instead of being required to use a single system-wide 'mixer' rate.
                        //It should also reduce latency a little.
wasapi_forcechannels "0"        //Attempts to force snd_numchannels instead of using the system's default channel count.
                                //For this to work, you will need to set wasapi_exclusive 1
wasapi_forcerate "0"    //Attempts to force snd_khz instead of using the system's default channel count.
                        //For this to work, you will need to set wasapi_exclusive 1

// OpenAL variables
s_al_speedofsound "343.3"       //Configures the speed of sound, in game units per second. This affects doppler.
s_al_static_listener "0"
s_al_velocityscale "1"  //Rescales velocity values, before doppler can be calculated.
s_al_reference_distance "120"   //This is the distance at which the sound is audiable with standard volume in the inverse distance models. Nearer sounds will be louder than the original sample.
s_al_distancemodel "2"  //Controls how sounds fade with distance.
                        //0: Inverse (most realistic)
                        //1: Inverse Clamped
                        //2: Linear (Quake-like)
                        //3: Linear Clamped
                        //4: Exponential
                        //5: Exponential Clamped
                        //6: None
s_al_dopplerfactor "1.0"        //Multiplies the strength of doppler effects.
s_al_use_reverb "1"     //Controls whether reverb effects will be used. Set to 0 to block them. Reverb requires gamecode to configure the reverb properties, other than underwater.
s_al_debug "0"          //Enables periodic checks for OpenAL errors.
s_al_disable "0"        //0: OpenAL works (generally as the highest priority).
                        //1: OpenAL will be used only when a specific device is selected.
                        //2: Don't allow ANY use of OpenAl.
                        //With OpenAL disabled, audio ouput will fall back to platform-specific output, avoiding miscilaneous third-party openal limitation bugs.

// Voice Chat
cl_voip_bitrate "3000"  //For codecs with non-specific bitrates, this specifies the target bitrate to use.
cl_voip_autogain "0"    //Attempts to normalize your voice levels to a standard level. Useful for lazy people, but interferes with voice activation levels.
cl_voip_noisefilter "1" //Enable the use of the noise cancelation filter.
cl_voip_codec ""        //0: speex(@11khz). 1: raw. 2: opus. 3: speex(@8khz). 4: speex(@16). 5:speex(@32). 6: pcma. 7: pcmu.
cl_voip_micamp "2"      //Amplifies your microphone when using voip.
cl_voip_ducking "0.5"   //Scales game audio by this much when someone is talking to you. Does not affect your speaker volume when you speak (minimum of cl_voip_capturingvol and cl_voip_ducking is used).
cl_voip_test "0"        //If 1, enables you to hear your own voice directly, bypassing the server and thus without networking latency, but is fine for checking audio levels. Note that sv_voip_echo can be set if you want to include latency and packetloss considerations, but setting that cvar requires server admin access and is thus much harder to use.
cl_voip_play "1"        //Enables voip playback. Value is a volume scaler.
cl_voip_showmeter "1"   //Shows your speech volume above the standard hud. 0=hide, 1=show when transmitting, 2=ignore voice-activation disable
cl_voip_capturingvol "0.5"      //Volume multiplier applied while capturing, to avoid your audio from being heard by others. Does not affect game volume when others speak (minimum of cl_voip_capturingvol and cl_voip_ducking is used).
cl_voip_vad_delay "0.3" //Keeps sending voice data for this many seconds after voice activation would normally stop
cl_voip_vad_threshhold "15"     //This is the threshhold for voice-activation-detection when sending voip data
cl_voip_send "0"        //Sends voice-over-ip data to the server whenever it is set.
                        //0: only send voice if +voip is pressed.
                        //1: voice activation.
                        //2: constantly send.
                        //+4: Do not send to game, only to rtp sessions.
cl_voip_capturedevice ""

// Sound controls
s_linearresample_stream "0"
s_linearresample "1"
snd_ignorecueloops "0"  //Ignores cue commands in wav files, for q3 compat.
s_device ""             //This is the sound device(s) to use, in the form of driver:device.
                        //If desired, multiple devices can be listed in space-seperated (quoted) tokens. _s_device_opts contains any enumerated options.
                        //In all seriousness, use the menu to set this if you wish to keep your hair.
s_localvolume "1"       //Sound level for sounds local or originating from the player such as firing and pain sounds.
s_mixerthread "1"       //When enabled sound mixing will be run on a separate thread. Currently supported only by directsound. Other drivers may unconditionally thread audio. Set to 0 only if you have issues.
s_inactive "1"          //Play sound while application is inactive (ie: tabbed out). Needs a snd_restart if changed.
s_doppler_max "2"       //Highest allowed doppler scale, to avoid things getting too weird.
s_doppler_min "0.5"     //Slowest allowed doppler scale.
s_doppler "0"           //Enables doppler, with a multiplier for the scale.
snd_ignoregamespeed "0" //When set, allows sounds to desynchronise with game time or demo speeds.
snd_playbackrate "1"    //Debugging cvar that changes the playback rate of all new sounds.
s_bits "16"
s_buffersize "0"
s_numspeakers "2"       //Number of hardware audio channels to use. FTE Quake supports up to 6.
s_eax "0"
s_swapstereo "0"
s_khz "22"              //Sound speed, in kilohertz. Common values are 11, 22, 44, 48. Values above 1000 are explicitly in hertz.
s_mixahead "0.1"        //Specifies how many seconds to prebuffer audio. Lower values give less latency, but might result in crackling. Different hardware/drivers have different tolerances, and this setting may be ignored completely where drivers are expected to provide their own tolerances.
s_show "0"
s_noextraupdate "0"
s_ambientfade "0"
s_ambientlevel "0"      //This controls the volume levels of automatic area-based sounds (like water or sky), and is quite annoying. If you're playing deathmatch you'll definitely want this OFF.
s_nominaldistance "1000"        //This cvar defines how far an attenuation=1 sound can be heard.
musicvolume ".02"       //Volume level for background music.
snd_loadasstereo "0"    //Force mono sounds to load as if stereo ones, to waste memory. Not normally useful.
s_loadas8bit "0"        //Downsample sounds on load as lower quality 8-bit sound, to save memory.
s_precache "1"
volume ".05"            //Volume level for game sounds (does not affect music, voice, or cinematics).
mastervolume "1"        //Additional multiplier for all other sounds.
nosound "0"             //Disable all sound from the engine. Cannot be overriden by configs or anything if set via the -nosound commandline argument.

// Joystick variables
joystick "0"

// input controls
in_rawinput_rdp "0"     //Activate Remote Desktop Protocol devices too.
in_rawinput_keyboard "0"        //Enables rawinput support for keyboards in XP onwards as well as just mice.
in_rawinput "1"         //Enables rawinput support for mice in XP onwards. Rawinput permits independant device identification (ie: splitscreen clients can each have their own mouse)
m_threshold_noforce "0"
m_accel_noforce "0"
in_simulatemultitouch "0"
in_nonstandarddeadkeys "1"      //Discard input events that result in multiple keys. Only the last key will be used. This results in behaviour that differs from eg notepad. To use a dead key, press it twice instead of the dead key followed by space.
in_builtinkeymap "1"
xinput_rightvibrator "0"
xinput_leftvibrator "0"
in_xinput "1"           //Enables the use of xinput for controllers.
                        //Note that if you have a headset plugged in, that headset will be used for audio playback if no specific audio device is configured.
in_dinput "0"           //Enables the use of dinput for mouse movements
cl_keypad "1"
joyradialdeadzone "1"   //Treat controller dead zones as a pair, rather than per-axis.
joyexponent "3"         //Scales joystick/controller sensitivity non-linearly to increase precision in the center.
                        //A value of 1 is linear.
joyupthreshold "0.118"
joyrollthreshold "0.118"        //Values reported near the center of the analog joystick/controller are often erroneous and undesired.
                                //The joystick threshholds are how much of the total values to ignore.
joyupsensitivity "1.0"
joyrollsensitivity "1.0"        //Scaler value for the controller when it is at its most extreme value
joysidethreshold "0.15"
joyyawthreshold "0.15"  //Values reported near the center of the analog joystick/controller are often erroneous and undesired.
                        //The joystick threshholds are how much of the total values to ignore.
joysidesensitivity "-1.0"
joyyawsensitivity "-1.0"        //Scaler value for the controller when it is at its most extreme value
joyforwardthreshold "0.15"
joypitchthreshold "0.15"        //Values reported near the center of the analog joystick/controller are often erroneous and undesired.
                                //The joystick threshholds are how much of the total values to ignore.
joyforwardsensitivity "-1.0"
joypitchsensitivity "1.0"       //Scaler value for the controller when it is at its most extreme value
joyadvaxisv_scale "1.0" //Because joyadvaxisx etc can be added together, this provides a way to rescale or invert an individual axis without affecting another with the same action.
joyadvaxisv "0"         //Provides a way to remap each joystick/controller axis.
                        //Should be set to one of: moveforward, moveback, lookup, lookdown, turnleft, turnright, moveleft, moveright, moveup, movedown, rollleft, rollright
joyadvaxisu_scale "1.0" //Because joyadvaxisx etc can be added together, this provides a way to rescale or invert an individual axis without affecting another with the same action.
joyadvaxisu "0"         //Provides a way to remap each joystick/controller axis.
                        //Should be set to one of: moveforward, moveback, lookup, lookdown, turnleft, turnright, moveleft, moveright, moveup, movedown, rollleft, rollright
joyadvaxisr_scale "1.0" //Because joyadvaxisx etc can be added together, this provides a way to rescale or invert an individual axis without affecting another with the same action.
joyadvaxisr "0"         //Provides a way to remap each joystick/controller axis.
                        //Should be set to one of: moveforward, moveback, lookup, lookdown, turnleft, turnright, moveleft, moveright, moveup, movedown, rollleft, rollright
joyadvaxisz_scale "1.0" //Because joyadvaxisx etc can be added together, this provides a way to rescale or invert an individual axis without affecting another with the same action.
joyadvaxisz "0"         //Provides a way to remap each joystick/controller axis.
                        //Should be set to one of: moveforward, moveback, lookup, lookdown, turnleft, turnright, moveleft, moveright, moveup, movedown, rollleft, rollright
joyadvaxisy_scale "1.0" //Because joyadvaxisx etc can be added together, this provides a way to rescale or invert an individual axis without affecting another with the same action.
joyadvaxisy "0"         //Provides a way to remap each joystick/controller axis.
                        //Should be set to one of: moveforward, moveback, lookup, lookdown, turnleft, turnright, moveleft, moveright, moveup, movedown, rollleft, rollright
joyadvaxisx_scale "1.0" //Because joyadvaxisx etc can be added together, this provides a way to rescale or invert an individual axis without affecting another with the same action.
joyadvaxisx "0"         //Provides a way to remap each joystick/controller axis.
                        //Should be set to one of: moveforward, moveback, lookup, lookdown, turnleft, turnright, moveleft, moveright, moveup, movedown, rollleft, rollright
m_accel_senscap "0"     //Used when m_accel_style is 1.
                        //Sets an upper limit on the amplified mouse movement. Great for tuning acceleration around lower velocities while still remaining in control of fast motion such as flicking.
m_accel_offset "0"      //Used when m_accel_style is 1.
                        //Acceleration will not be active until the mouse movement exceeds this speed (counts per millisecond). Negative values are supported, which has the effect of causing higher rates of acceleration to happen at lower velocities.
m_accel_power "2"       //Used when m_accel_style is 1.
                        //Values 1 or below are dumb. 2 is linear and the default. 99% of accel users use this. Above 2 begins to amplify exponentially and you will get more acceleration at higher velocities. Great if you want low accel for slow movements, and high accel for fast movements. Good in combination with a sensitivity cap.
m_accel_style "1"       //1 = Quake Live mouse acceleration, 0 = Old style accelertion.
m_accel "0"             //Values >0 will amplify mouse movement proportional to velocity. Small values have great effect. A lot of good Quake Live players use around the 0.1-0.2 mark, but this depends on your mouse CPI and polling rate.
m_touchmajoraxis "1"    //When using a touchscreen, use only the major axis for strafing.
m_slidethreshold "10"   //How far your finger needs to move to be considered a slide event (touchscreens).
m_fatpressthreshold "0.2"       //How fat your thumb has to be to register a fat press (touchscreens).
m_touchstrafe "1"       //0: entire screen changes angles only.
                        //1: right hand side controls strafing.
                        //2:left hand side strafes.
m_forcewheel_threshold "32"     //Mousewheel graduations smaller than this will not trigger mousewheel deltas.
m_forcewheel "1"        //0: ignore mousewheels in apis where it is abiguous.
                        //1: Use mousewheel when it is treated as a third axis. Motion above a threshold is ignored, to avoid issues with an unknown threshold.
                        //2: Like 1, but excess motion is retained. The threshold specifies exact z-axis distance per notice.
m_filter "1"

// Server Browser Vars
slist_cacheinfo "0"
slist_writeservers "1"
seta sb_showtimelimit "0"
seta sb_showfraglimit "0"
sb_showplayers "1"
seta sb_showgamedir "0"
seta sb_showmap "0"
seta sb_showaddress "0"
seta sb_showping "1"
sb_sortcolumn "-p"
sb_hideproxies "1"
sb_hidequakeworld "0"
sb_hidenetquake "0"
sb_hidedead "1"
sb_hidefull "0"
sb_hidenotempty "0"
sb_hideempty "0"
sb_alpha "0.7"

// Video Capture Controls
capturesoundchannels "2"
capturesoundbits "16"
capturethrottlesize "0" //If set, capturing will slow down significantly if there is less disk space available than this cvar (in mb). This is useful when recording a stream to (ram)disk while another program is simultaneously consuming frames.
capturecodec "tga"      //the compression/encoding codec to use.
                        //With (win)avi, this should be a fourcc code like divx or xvid, may be blank for raw video.
                        //With raw capturing, this should be one of tga,png,jpg,pcx (ie: screenshot extensions).
capturedriver ""        //The driver to use to capture the demo. Use the capture command with no arguments for a list of drivers.
capturedemoheight "0"   //When using capturedemo, this specifies the height of the FBO image used.
capturedemowidth "0"    //When using capturedemo, this specifies the width of the FBO image used. Can be larger than your physical monitor.
capturerate "30"        //The framerate of the video to capture
capturesound "1"        //Enables the capturing of game voice. If not using capturedemo, this can be combined with cl_voip_test to capture your own voice.
capturemessage ""

// Gimmicks
tts_mode "1"            //Text to speech
                        //0: off
                        //1: Read only chat messages with a leading 'tts ' prefix.
                        //2: Read all chat messages
                        //3: Read every single console print.

// Media player things
music_fade "1"
media_repeat "1"
media_shuffle "1"

// compat
set music_playlist_sampleposition5 "-1"
set music_playlist_list5 ""
set music_playlist_sampleposition4 "-1"
set music_playlist_list4 ""
set music_playlist_sampleposition3 "-1"
set music_playlist_list3 ""
set music_playlist_sampleposition2 "-1"
set music_playlist_list2 ""
set music_playlist_sampleposition1 "-1"
set music_playlist_list1 ""
set music_playlist_sampleposition0 "-1"
set music_playlist_list0 ""
music_playlist_index "-1"

// Menu thingumiebobs
m_helpismedia "0"
seta m_preset_chosen "1"

// Console variables
con_echochat "0"

// server rankings
rank_parms_last "31"    //Mod setting: the index of the last parm to be saved. Clamped to 32.
rank_parms_first "0"    //Mod setting: first parm saved
rank_filename ""        //Specifies which file to use as a rankings database. Enables the ranking system if set.
rank_needlogin "0"      //If set to 1, prohibits players from joining if they're not yet registered. This will require an external mechanism to register users, presumably via rcon.
rank_autoadd "1"        //Automatically register players into the ranking system

// MVD Streaming
qtv_password ""
qtv_maxstreams "0"      //This is the maximum number of QTV clients/proxies that may be directly connected to the server. If empty then there is no limit. 0 disallows any streaming.

// Server MVD cvars
sv_demo_write_csqc ""   //Writes a copy of the csprogs into recorded demos. This ensures that the demo can be played back despite future gamecode changes.
sv_demoAutoPrefix "auto_"
sv_demoAutoRecord "0"   //If set, automatically record demos.
                        //-1: record on client connection only.
                        //1+: record once there's this many active players on the server (or if connected to a remote server).
sv_demoAutoCompress ""  //Specifies whether to compress demos as they're recorded.
                        //0 = no compression.
                        //1 = gzip compression.
set sv_demoExtensions ""        //Enables protocol extensions within MVDs. This will cause older/non-fte clients to error upon playback.
                                //0: off.
                                //1: all extensions.
                                //2: extensions also supported by a certain other engine.
sv_demoExtraNames ""
sv_demotxt "1"
sv_demoSuffix ""
sv_demoPrefix ""
sv_demoDirAlt ""        //Provides a fallback directory name for demo downloads, for when sv_demoDir doesn't contain the requested demo.
sv_demoDir "demos"
sv_demoClearOld "0"     //Automatically delete demos to keep the demos count reasonable.
sv_demoMaxDirAge "0"    //Maximum allowed age for demos, any older demos will be deleted when sv_demoClearOld is set (this doesn't prevent recording new demos).
sv_demoMaxDirCount "500"        //Maximum allowed serverside mvds to record. Set to 0 to remove the limit. New demos cannot be recorded once this many demos have already been recorded.
sv_demoMaxDirSize "100mb"       //Maximum allowed serverside storage space for mvds. set to blank to remove the limit. New demos cannot be recorded once this size is reached.
sv_demoMaxSize ""       //Demos will be truncated to be no larger than this size.
sv_demoCacheSize "0x80000"
sv_demoUseCache ""      //If set, demo data will be flushed only periodically
sv_demoPings "10"       //Interval between ping updates in mvds
sv_demofps "30"

// serverinfo variables
sv_motd4 ""
sv_motd3 ""
sv_motd2 ""
sv_motd1 ""
sv_pupglow ""           //Instructs clients to enable hexen2-style powerup pulsing.
fbskins ""
allow_splitscreen ""    //Specifies whether clients can use splitscreen extensions to dynamically add additional clients. This only affects remote clients and not the built-in client.
                        //Clients may need to reconnect in order to add seats when this is changed.
allow_skybox ""
watervis ""
deathmatch "0"
hostname "localhost"
seta sv_playerslots ""  //Specify maximum number of player/spectator/bot slots, new value takes effect on the next map (this may result in players getting kicked). This should generally be set to maxclients+maxspectators. Leave blank for a default value.
                        //Maximum value of 255. Values above 16 will result in issues with vanilla NQ clients. Effective values other than 32 will result in issues with vanilla QW clients.
maxspectators "8"       //Specify the maximum number of spectators allowed on the server at once. Can be changed mid-map.
maxclients "1"          //Specify the maximum number of players allowed on the server at once. Can be changed mid-map.
samelevel ""
skill "0"
set coop ""
teamplay "0"
timelimit "0"
fraglimit "0"

// Backwards compatability
sv_brokenmovetypes "0"  //Emulate vanilla quakeworld by forcing MOVETYPE_WALK on all players. Shouldn't be used for any games other than QuakeWorld.

// server voting
votetime "10"           //Votes will be discarded after this many minutes
votepercent "-1"        //At least this percentage of players must vote the same way for the vote to pass.
voteminimum "4"         //At least this many players must vote the same way for the vote to pass.
votelevel "0"           //This is the restriction level of commands that players may vote for. You can reconfigure commands, cvars, or aliases individually. Additionally, aliases can be configured via aliaslevel to be executed at a different level from their restriction level. This can be used to indirectly allow voting for 'map dm4' for instance, without allowing people to vote for every map.

// Access controls
cmd_allowaccess "0"
cmd_gamecodelevel "(29)"

// server permission variables
sv_specprint "3"        //Bitfield that controls which player events spectators see when tracking that player.
                        //&1: spectators will see centerprints.
                        //&2: spectators will see sprints (pickup messages etc).
                        //&4: spectators will receive console commands, this is potentially risky.
                        //Individual spectators can use 'setinfo sp foo' to limit this setting.
secure ""
allow_download_other "0"        //0 blocks downloading of any file that was not covered by any of the directory download blocks.
allow_download_copyrighted "0"  //0 blocks download of packages that are considered copyrighted. Specifically, this means packages with a leading 'pak' prefix on the filename.
                                //If you take your copyrights seriously, you should also set allow_download_pakmaps 0 and allow_download_pakcontents 0.
allow_download_root "0" //If set, enables downloading from the root of the gamedir (not the basedir). This setting has a lower priority than extension-based checks.
allow_download_wads "1" //0 blocks downloading of any file in the wads/ directory, or is in the root directory with the extension .wad
set allow_download_refpackages "0"      //If set to 1, packages that contain files needed during spawn functions will be become 'referenced' and automatically downloaded to clients.
                                        //This cvar should probably not be set if you have large packages that provide replacement pickup models on public servers.
                                        //The path command will show a '(ref)' tag next to packages which clients will automatically attempt to download.
allow_download_packages "1"     //if 1, permits downloading files (from root directory or elsewhere) with known package extensions (eg: pak+pk3). Packages with a name starting 'pak' are covered by allow_download_copyrighted as well.
allow_download_locs "1" //0 blocks downloading of any file in the locs/ directory
allow_download_configs "0"      //1 allows downloading of config files, either with the extension .cfg or in the subdir configs/.
                                //^&C0THIS IS DANGEROUS AS IT CAN ALLOW PEOPLE TO READ YOUR RCON PASSWORD ETC.
allow_download_textures "1"     //0 blocks downloading of any file in the textures/ directory
set allow_download_pakcontents "1"      //controls whether clients connected to this server are allowed to download files from within packages.
                                        //Does NOT implicitly allow downloading bsps, set allow_download_pakmaps to enable that.
                                        //While treating each file contained within packages is often undesirable, this is often needed for compatibility with legacy clients (despite it potentially allowing copyright violations).
allow_download_pakmaps "0"      //If 1, permits downloading of maps from within packages. This is normally disabled in order to prevent copyrighted content from being downloaded.
allow_download_demos "1"        //0 blocks downloading of any file in the demos/ directory
allow_download_maps "1" //0 blocks downloading of any file in the maps/ directory
allow_download_sounds "1"       //0 blocks downloading of any file in the sound/ directory
allow_download_models "1"       //0 blocks downloading of any file in the progs/ or models/ directory
allow_download_skins "1"        //0 blocks downloading of any file in the skins/ directory
allow_download "1"      //If 1, permits downloading. Set to 0 to unconditionally block *ALL* downloads from this server. You may wish to set sv_dlURL if you wish clients to still be able to download content.
sv_dlURL ""             //Provides clients with an external url from which they can obtain pk3s/packages from an external http server instead of having to download over udp.
sv_banproxies "0"       //If enabled, anyone connecting via known proxy software will be refused entry. This should aid with blocking aimbots, but is only reliable for certain public proxies.
sv_cmdlikercon "0"
sv_chatfilter "0"
sv_voip_record "0"      //Record voicechat into mvds. Requires player support. 0=noone, 1=everyone, 2=spectators only
sv_voip_echo "0"        //Echo voice packets back to their sender, a debug/test setting.
sv_voip "1"             //Enable reception of voice packets.

// Server Permissions
sv_cheats "0"

// server control variables
sv_savefmt ""           //Specifies the format used for the saved game.
                        //0=legacy.
                        //1=fte
                        //2=binary
sv_autosave "5"         //Interval for autosaves, in minutes. Set to 0 to disable autosave.
sv_nopvs "0"            //Set to 1 to ignore pvs on the server. This can make wallhacks more dangerous, so should only be used for debugging.
sv_nailhack "1"         //If set to 1, disables the nail entity networking optimisation. This hack was popularised by qizmo which recommends it for better compression. Also allows clients to interplate nail positions and add trails.
sv_minping ""           //Simulate fake lag for any players with a ping under the value specified here. Value is in milliseconds.
sv_maxdrate "500000"    //This cvar controls the maximum number of bytes sent to each player per second while that player is downloading.
                        //If this cvar is set to 0, there will be NO CAP for download rates (if the user's drate is empty/0 too, then expect really fast+abusive downloads that could potentially be considered denial of service attacks)
sv_maxrate "50000"      //This controls the maximum number of bytes any indivual player may receive (when not downloading). The individual user's rate will also be controlled by the user's rate cvar.
pausable "1"
pr_allowbutton1 "1"     //The button1 field is believed to have been intended to work with the +use command, but it was never hooked up. In NetQuake, this field was often repurposed for other things as it was not otherwise used (and cannot be removed without breaking the crc), while third-party QuakeWorld engines did decide to implement it as believed was intended. As a result, this cvar only applies to QuakeWorld mods and a value of 1 is only likely to cause issues with NQ mods that were ported to QW.
sv_csqcdebug "0"        //Inject packet size information for data directed to csqc.
sv_csqc_progname "csprogs.dat"
sv_cullentities_trace ""        //Attempt to cull non-player entities using tracelines as an extreeme anti-wallhack.
sv_cullplayers_trace "" //Attempt to cull player entities using tracelines as an anti-wallhack.
sv_phs "1"              //If 1, do not use the phs. It is generally better to use sv_calcphs instead, and leave this as 1.
sv_calcphs "2"          //Enables culling of sound effects. 0=always skip phs. Sounds are globally broadcast. 1=always generate phs. Sounds are always culled. On large maps the phs will be dumped to disk. 2=On large single-player maps, generation of phs is skipped. Otherwise like option 1.
sv_highchars "1"
filterban "1"           //If 0, players will be kicked by default unless there is a rule that allows them. Also affects the default action of addip.
sv_showconnectionlessmessages "0"       //Display a line describing each connectionless message that arrives on the server. Primarily a debugging feature, but also potentially useful to admins.
sv_reportheartbeats "2" //Print a notice each time a heartbeat is sent to a master server. When set to 2, the message will be displayed once.
fraglog_public "1"      //Enables support for connectionless fraglog requests
sv_use_dns ""           //Performs a reverse-dns lookup in order to report more info about where clients are connecting from.
sv_reconnectlimit "0"   //Blocks dupe connection within the specified length of time .
sv_listen_dp "0"        //Allows the server to respond with the DP-specific handshake protocol.
                        //Warning: this can potentially get confused with quake2, and results in race conditions with both vanilla netquake and quakeworld protocols.
                        //On the plus side, DP clients can usually be identified correctly, enabling a model+sound limit boost.
sv_listen_nq "2"        //Allow new (net)quake clients to connect to the server.
                        //0 = don't let them in.
                        //1 = allow them in (WARNING: this allows 'qsmurf' DOS attacks).
                        //2 = accept (net)quake clients by emulating a challenge (as secure as QW/Q2 but does not fully conform to the NQ protocol).
sv_listen_qw "1"        //Specifies whether normal clients are allowed to connect.
sv_public "0"           //-1: Fully blocks all inbound connections.
                        //0: Disable subscribing to master servers (for private lan-only games).
                        //1: Subscribe to public master servers. Your IP address will be listed publicly. Make sure your Router/NAT+Firewall are set to allow inbound connections.
                        //2: Subscribe to a broker master, allowing firewall hole punching.
sv_serverip ""          //Set this cvar to the server's public ip address if the server is behind a firewall and cannot detect its own public address. Providing a port is required if the firewall/nat remaps it, but is otherwise optional.
sv_guidkey ""           //If set, clients will calculate their GUID values against this string instead of the server's IP address. This allows consistency between multiple servers (for stats tracking), but do NOT treat the client's GUID as something that is secure.
sv_resetparms "0"
sv_aim "1"              //The value should be cos(angle), where angle is the greatest allowed angle from which to deviate from the direction the shot would have been along. Values >1 thus disable auto-aim. This can be overridden with setinfo aim 0.73.
zombietime "2"          //Client slots will not be reused for this number of seconds.
spawn ""
sv_limittics "3"        //The maximum number of ticks that may be run within a frame, to allow the server to catch up if it stalled or if sv_maxtic is too low.
sv_maxtic "0.1"         //The maximum interval between running physics frames. If the value is too low, multiple physics interations might be run at a time (based upon sv_limittics). Set to 0 for fixed-interval ticks, which may be required if ODE is used.
sv_mintic "0"           //The minimum interval between running physics frames.
spectator_password ""
sv_crypt_rcon ""        //Controls whether the rcon password must be hashed or not. Hashed passwords also partially prevent replay attacks, but does NOT prevent malicious actors from reading the commands/results.
                        //0: completely insecure. ONLY allows plain-text passwords. Do not use.
                        //1: Mandatory hashing (recommended).
                        //Empty: Allow either, whether the password is secure or not is purely the client's responsibility/fault. Only use this for comptibility with old clients.
sv_rconlim "4"          //Blocks repeated (invalid) rcon attempts.
pext_ezquake_verfortrans "999999999"    //ezQuake does not implement PEXT_TRANS properly. This is the version of ezquake required for PEXT_TRANS to be allowed. This was still broken when I wrote this description, hence the large value.
pext_ezquake_nochunks "0"       //Prevents ezquake clients from being able to use the chunked download extension. This sidesteps numerous ezquake issues, and will make downloads slower but more robust.
sv_floodprotect_sendmessage ""
sv_floodprotect_suicide "1"
sv_floodprotect_silencetime "10"
sv_floodprotect_messages "4"
sv_floodprotect_interval "4"
sv_floodprotect "1"
sv_pure ""              //The most evil cvar in the world, many clients will ignore this.
                        //0=standard quake rules.
                        //1=clients should prefer files within packages present on the server.
                        //2=clients should use *only* files within packages present on the server.
                        //Due to quake 1.01/1.06 differences, a setting of 2 only works in total conversions.
sv_protocol_nq ""       //Specifies the default protocol to use for new NQ clients. This is only relevent for clients that do not report their supported protocols. Supported values are
                        //0 = autodetect
                        //15 = vanilla
                        //666 = fitzquake
                        //999 = rmq protocol
                        //The sv_bigcoords cvar forces upgrades as required.
sv_pushplayers "0"
sv_userinfo_bytelimit "8192"    //This is the maximum number of bytes that may be stored into each user's userinfo. Note that this includes key names too.
sv_userinfo_keylimit "128"      //This is the maximum number of userinfo keys each user may create.
sv_realip_timeout "10"
sv_realiphostname_ipv6 ""       //This is the server's public ip:port. This is needed for realip to work when the autodetected/local ip is not globally routable
sv_realiphostname_ipv4 ""       //This is the server's public ip:port. This is needed for realip to work when the autodetected/local ip is not globally routable
sv_realip_kick "0"      //Kicks clients if their realip could not be validated to the level specified by sv_getrealip.
sv_getrealip "0"        //Attempt to obtain a more reliable IP for clients, rather than just their proxy.
                        //0: Don't attempt.
                        //1: Unreliable checks.
                        //2: Validate if possible.
                        //3: Mandatory validation.
sv_playermodelchecks "0"
sv_antilag_frac ""
sv_antilag ""           //Attempt to backdate impacts to compensate for lag via the MOVE_ANTILAG feature.
                        //0=completely off.
                        //1=mod-controlled (default).
                        //2=forced, which might break certain uses of traceline.
                        //3=Also attempt to recalculate trace start positions to avoid lagged knockbacks.
sv_fullredirect ""      //This is the ip:port to redirect players to when the server is full
seta maxpitch "80"      //Assumed to be 80
seta minpitch "-70"     //Assumed to be -70
sv_mapcheck "1"
sv_spectalk "1"
sv_autooffload "0"      //Automatically start the server in a separate process, so that sporadic or persistent gamecode slowdowns do not affect visual framerates. Note: Offloaded servers have separate cvar states which may complicate usage.

// SQL Defaults
sv_sql_defaultdb ""
sv_sql_password ""
sv_sql_username ""
sv_sql_host "127.0.0.1"
sv_sql_driver ""

// server physics variables
seta sv_nqplayerphysics "0"     //Disable player prediction and run NQ-style player physics instead. This can be used for compatibility with mods that expect exact behaviour.
sv_gamespeed "1"
sv_reliable_sound "0"   //Causes all sounds to be sent reliably, so they will not be missed due to packetloss. However, this will cause them to be delayed somewhat, and slightly bursty. This can be overriden using the 'rsnd' userinfo setting (either forced on or forced off). Note: this does not affect sounds attached to particle effects.
sv_compatiblehulls "1"
pm_edgefriction "2"     //Default value of 2
pm_walljump ""          //Allows the player to bounce off walls while arborne.
seta pm_stepdown ""     //Causes physics to stick to the ground, instead of constantly losing traction whiloe going down steps.
pm_pground ""           //Use persisten onground state instead of recalculating every frame.^&E0Do NOT use with nq mods, as most nq mods will interfere with onground state, resulting in glitches.
pm_airstep ""           //Allows players to step up while jumping. This makes stairs more graceful but also increases potential jump heights.
pm_slidyslopes ""       //Replicates NQ behaviour, where players will slowly slide down ramps
pm_slidefix ""          //Fixes an issue when walking down slopes (ie: so they act more like slopes and not a series of steps)
pm_ktjump ""
pm_flyfriction ""       //Amount of friction that applies in fly or 6dof mode. Empty means 4.
pm_watersinkspeed ""    //This is the speed that players will sink at while inactive in water. Empty means 60.
seta pm_bunnyspeedcap ""        //0 or 1, ish. If the player is traveling faster than this speed while turning, their velocity will be gracefully reduced to match their current maxspeed. You can still rocket-jump to gain high velocity, but turning will reduce your speed back to the max. This can be used to disable bunny hopping.
set sv_bigcoords ""     //Uses floats for coordinates instead of 16bit values.
                        //Also boosts angle precision, so can be useful even on small maps.
                        //Affects clients thusly:
                        //QW: enforces a mandatory protocol extension
                        //DP: enables DPP7 protocol support
                        //NQ: uses RMQ protocol (protocol 999).
sv_gameplayfix_spawnbeforethinks "0"    //Fixes an issue where player thinks (including Pre+Post) can be called before PutClientInServer. Unfortunately at least one mod depends upon PreThink being called first in order to correctly determine spawn positions.
dpcompat_noretouchground "0"    //Prevents entities that are already standing on an entity from touching the same entity again.
sv_gameplayfix_trappedwithin "0"        //Blocks further entity movement when an entity is already inside another entity. This ensures that bsp precision issues cannot allow the entity to completely pass through eg the world.
sv_gameplayfix_grenadebouncedownslopes "0"      //MOVETYPE_BOUNCE speeds are calculated relative to the impacted surface, instead of the vertical, reducing the chance of grenades just sitting there on slopes.
sv_gameplayfix_stepdown "0"     //Attempt to step down steps, instead of only up them. Affects non-predicted movetype_walk.
sv_gameplayfix_multiplethinks "1"       //Enables multiple thinks per entity per frame so small nextthink times are accurate. QuakeWorld mods expect a value of 1, while NQ expects 0.
sv_gameplayfix_noairborncorpse "0"
pm_stepheight ""        //If empty, the value 18 will be used instead. This is the size of the step you can step up or down.
sv_sound_land "demon/dland2.wav"
sv_sound_watersplash "misc/h2ohit1.wav"
sv_wallfriction "1"     //Additional friction when running into walls
sv_waterfriction "4"
sv_friction "4"
sv_wateraccelerate "10"
sv_airaccelerate "0.7"
seta sv_accelerate "10"
sv_spectatormaxspeed "500"
seta sv_maxspeed "320"
sv_stopspeed "100"
sv_gravity "800"
sv_maxvelocity "2000"

// Progs variables
sv_gameplayfix_setmodelsize_qw "0"      //The setmodel builtin will act as a setsize for QuakeWorld mods also.
sv_gameplayfix_setmodelrealbox "0"      //Vanilla setmodel will setsize the entity to a hardcoded size for non-bsp models. This cvar will always use the real size of the model instead, but will require that the server actually loads the model.
sv_gameplayfix_honest_tracelines "1"
pr_ssqc_memsize "-1"    //The ammount of memory available to the QC vm. This has a theoretical maximum of 1gb, but that value can only really be used in 64bit builds. -1 will attempt to use some conservative default, but you may need to increase it. Consider also clearing pr_fixbrokenqccarrays if you need to change this cvar.
pr_coreonerror "1"
pr_overridebuiltins "1"
pr_nonetaccess "0"      //Block all direct access to network buffers (the writebyte builtin and friends will ignore the call).
dpcompat_nopreparse "0" //Xonotic uses svc_tempentity with unknowable lengths mixed with other data that needs to be translated. This cvar disables any attempt to translate or pre-parse network messages, including disabling nq/qw cross compatibility. NOTE: because preparsing will be disabled, messages might not get backbuffered correctly if too much reliable data is written.
dpcompat_precachesoundhack "0"  //Changes the behaviour of only the ssqc's precache_sound to return a precache index. precache_model and csqc's precaches are unchanged.
pr_compatabilitytest "0"        //Only enables builtins if the extension they are part of was queried.
sv_progs "progs.dat"    //Specifies the filename of the gamecode to use serverside. Empty means autodetect - typically loading either progs or qwprogs depending on gamedir priority, then based upon deathmatch settings.
noexit "0"
temp1 "0"
saved4 "0"
saved3 "0"
saved2 "0"
saved1 "0"
savedgamecfg "0"
scratch4 "0"
scratch3 "0"
scratch2 "0"
scratch1 "0"
gamecfg "0"
nomonsters "0"
addon15 ""
addon14 ""
addon13 ""
addon12 ""
addon11 ""
addon10 ""
addon9 ""
addon8 ""
addon7 ""
addon6 ""
addon5 ""
addon4 ""
addon3 ""
addon2 ""
addon1 ""
addon0 ""
pr_no_parsecommand "0"  //Provides a way around invalid mod usage of SV_ParseClientCommand, eg xonotic.
pr_no_playerphysics "0" //Prevents support of the 'SV_PlayerPhysics' QC function. This allows servers to prevent needless breakage of player prediction.
pr_maxedicts "16384"    //Maximum number of entities spawnable on the map at once. Low values will crash the server on some maps/mods. High values will result in excessive memory useage (see pr_ssqc_memsize). Illegible server messages may occur with old/other clients above 32k. FTE's network protocols have a maximum at a little over 4 million. Please don't ever make a mod that actually uses that many...
pr_imitatemvdsv "0"     //Enables mvdsv-specific builtins, and fakes identifiers so that mods made for mvdsv can run properly and with the full feature set.
pr_precachepic_slow "0" //Legacy setting. Should be set to 0 where supported.
pr_autocreatecvars "1"  //Implicitly create any cvars that don't exist when read.
utf8_enable "0"         //When 1, changes the qc builtins to act upon codepoints instead of bytes. Do not use unless com_parseutf8 is also set.
pr_fixbrokenqccarrays "0"       //As part of its nq/qw/h2/csqc support, FTE remaps QC fields to match an internal order. This is a faster way to handle extended fields. However, some QCCs are buggy and don't report all field defs.
                                //0: do nothing. QCC must be well behaved.
                                //1: Duplicate engine fields, remap the ones we can to known offsets. This is sufficient for QCCX/FrikQCC mods that use hardcoded or even occasional calculated offsets (fixes ktpro).
                                //2: Scan the mod for field accessing instructions, and assume those are the fields (and that they don't alias non-fields). This can be used to work around gmqcc's WTFs (fixes xonotic).
pr_sourcedir "src"      //Subdirectory where your qc source is located. Used by the internal compiler and qc debugging functionality.
pr_enable_profiling "0" //Enables profiling support. Will run more slowly. Change the map and then use the profile_ssqc/profile_csqc commands to see the results.
pr_enable_uriget "1"    //Allows gamecode to make direct http requests
pr_gc_threaded "1"      //Says whether to use a separate thread for tempstring garbage collections. This avoids main-thread stalls but at the expense of more memory usage.
pr_brokenfloatconvert "0"
pr_droptofloorunits "256"       //Distance that droptofloor is allowed to drop to be considered successul.
dpcompat_strcat_limit ""        //When set, cripples strcat (and related function) string lengths to the value specified.
                                //Set to 16383 to replicate DP's limit, otherwise leave as 0 to avoid limits.
dpcompat_findradiusarealinks "0"        //Use the world collision info to accelerate findradius instead of looping through every single entity. May actually be slower for large radiuses, or fail to find entities which have not been linked properly with setorigin.
sv_gameplayfix_droptofloorstartsolid "0"        //When droptofloor fails, this causes a second attemp, but with traceline instead.
sv_gameplayfix_nolinknonsolid "1"       //When 0, setorigin et al will not link the entity into the collision nodes (which is faster, especially if you have a lot of non-solid entities. When 1, allows entities to freely switch between .solid values (except for SOLID_BSP) without relinking. A lot of DP mods assume a value of 1 and will bug out otherwise, while 0 will restore a bugs present in various mods.
sv_gameplayfix_findradiusdistancetobox "0"      //When 1, findradius checks to the nearest part of the entity instead of only its origin, making it find slightly more entities.
sv_gameplayfix_blowupfallenzombies "0"  //Allow findradius to find non-solid entities. This may break certain mods. It is better for mods to use FL_FINDABLE_NONSOLID instead.

// Ground Clutter
r_clutter_distance "1024"       //Distance at which clutter will become invisible.
r_clutter_density "0"   //Scaler for clutter counts. 0 disables clutter completely.
                        //Clutter requires shaders with 'fte_clutter MODEL SPACING SCALEMIN SCALEMAX ZOFS ANGLEMIN ANGLEMAX' terms

// Particle effects
r_grenadetrail "1"
r_rockettrail "1"
gl_part_flame "0"       //Enable particle emitting from models. Mainly used for torch and flame effects.
seta r_part_classic_opaque "0"  //Disables transparency on classic particles, for the oldskool look.
seta r_part_classic_expgrav "10"        //Scaler for how fast classic explosion particles should accelerate due to gravity. 1 for like vanilla, 10 for like zquake.
seta r_part_density "1"
r_part_contentswitch "1"        //Enable particle effects to change based on content (ex. water).
r_part_beams "1"
r_part_sparks_textured "1"
r_part_sparks_trifan "1"
r_part_sparks "1"
r_part_maxdecals "8192"
r_part_maxparticles "65536"
r_particle_tracelimit "0x7fffffff"      //Number of traces to allow per frame for particle physics.
r_part_rain_quantity "1"
r_part_rain "1"         //Enable particle effects to emit off of surfaces. Mainly used for weather or lava/slime effects.
r_bouncysparks "1"      //Enables particle interaction with world surfaces, allowing for bouncy particles, stains, and decals.
seta r_particledesc "classic weather haze-lite-separated/haze-lite-customTF-lasercannon haze"
seta r_particlesystem "script"
r_decal_noperpendicular "1"     //When enabled, decals will not be generated on planes at a steep angle from clipped decal orientation.

// bloom
r_bloom_initialscale "1"        //Initial scaling factor for bloom. should be either 1 or 0.5
r_bloom_downsize "0"    //Technically more correct with a value of 1, but you probably won't notice.
r_bloom_size "4"        //Target bloom kernel size (assuming a video width of 320).
r_bloom_filter "0.7 0.7 0.7"    //Controls how bright the image must get before it will bloom (3 separate values, in RGB order).
r_bloom_retain "1"      //How much of the regular scene to retain when bloom is active.
seta r_bloom "0"        //Enables bloom (light bleeding from bright objects). Fractional values reduce the amount shown.

// gl blacklists
gl_blacklist_generatemipmap "1" //Self-generate mipmaps, instead of letting the graphics driver do it.
gl_blacklist_texture_compression "0"    //When enabled, blocks recognition of all compressed-texture formats. Does NOT block support for gl3 texture types like e5bgr9 nor rgba16f.

// D3D Renderer Options
d3d_hlsl "1"

// Vulkan-Specific Renderer Options
vk_amd_rasterization_order ""   //Enables the use of relaxed rasterization ordering, for a small speedup at the minor risk of a little zfighting.
vk_khr_push_descriptor ""       //Enables better descriptor streaming.
vk_khr_dedicated_allocation ""  //Flag vulkan memory allocations as dedicated, where applicable.
vk_khr_get_memory_requirements2 ""      //Enable extended memory info querires
vk_loadglsl ""          //Enable direct loading of glsl, where supported by drivers. Do not use in combination with vk_debug 2 (vk_debug should be 1 if you want to see any glsl compile errors). Don't forget to do a vid_restart after.
vk_usememorypools ""    //Allocates memory pools for sub allocations. Vulkan has a limit to the number of memory allocations allowed so this should always be enabled, however at this time FTE is unable to reclaim pool memory, and would require periodic vid_restarts to flush them.
vk_waitfence ""         //Waits on fences, instead of semaphores. This is more likely to result in gpu stalls while the cpu waits.
vk_busywait ""          //Force busy waiting until the GPU finishes doing its thing.
vk_dualqueue ""         //Attempt to use a separate queue for presentation. Blank for default.
vk_debug "0"            //Register a debug handler to display driver/layer messages. 2 enables the standard validation layers.
vk_submissionthread ""  //Execute submits+presents on a thread dedicated to executing them. This may be a significant speedup on certain drivers.
vk_stagingbuffers ""    //Configures which dynamic buffers are copied into gpu memory for rendering, instead of reading from shared memory. Empty for default settings.
                        //Accepted chars are u(niform), e(lements), v(ertex), 0(none).

// Screen Options
cl_yieldcpu "0"         //Attempt to yield between frames. This can resolve issues with certain drivers and background software, but can mean less consistant frame times. Will reduce power consumption/heat generation so should be set on laptops or similar (over-hot/battery powered) devices.
cl_idlefps "30"         //This is the maximum framerate to attain while idle/paused/unfocused.
cl_maxfps "528"         //Sets the maximum allowed framerate. If you're using vsync or want to uncap framerates entirely then you should probably set this to 0. Set cl_yieldcpu 0 if you're trying to benchmark.
cl_hudswap "0"
cl_pure "0"             //0=standard quake rules.
                        //1=clients should prefer files within packages present on the server.
                        //2=clients should use *only* files within packages present on the server.
                        //Due to quake 1.01/1.06 differences, a setting of 2 is only reliable with total conversions.
                        //If sv_pure is set, the client will prefer the highest value set.
seta cl_sbar "0"
cl_shownet "0"          //Debugging var. 0 shows nothing. 1 shows incoming packet sizes. 2 shows individual messages. 3 shows entities too.
scr_showobituaries "0"
scr_diskicontimeout "0.3"
scr_showdisk_y "0"
scr_showdisk_x "-24"
scr_showdisk "0"
scr_neticontimeout "0.3"
show_speed_y "-9"
show_speed_x "-1"
show_speed "0"
cl_gameclock_y "1"
cl_gameclock_x "600"
cl_gameclock "2"
cl_clock_y "-1"
cl_clock_x "0"
cl_clock "1"
show_fps_y "-1"
show_fps_x "-5"
show_fps "1"            //Displays the current framerate on-screen.
                        //1: framerate average over a second.
                        //2: Slowest frame over the last second (the game will play like shit if this is significantly lower than the average).
                        //3: Shows the rate of the fastest frame (not very useful).
                        //4: Shows the current frame's timings (this depends upon timer precision).
                        //5: Display a graph of how long it took to render each frame, large spikes are BAD BAD BAD.
                        //6: Displays the standard deviation of the frame times, if its greater than 3 then something is probably badly made, or you've a virus scanner running...
                        //7: Framegraph, for use with slower frames.
scr_loadingscreen_aspect "0"    //Controls the aspect of levelshot images.
                                //0: Use source image's aspect.
                                //1: Force 4:3 aspect (ignore image's aspect), for best q3 compat.
                                //2: Ignore aspect considerations and just smear it over the entire screen.
                                //-1: Disable levelshot use.
scr_loadingscreen_scale_limit "2"
scr_loadingscreen_scale "1"
scr_loadingscreen_picture "gfx/loading"
scr_showloading "1"
scr_loadingrefresh "0"  //Force redrawing of the loading screen, in order to display EVERY resource that is loaded
set con_stayhidden "0"  //0: allow console to pounce on the user
                        //1: console stays hidden unless explicitly invoked
                        //2:toggleconsole command no longer works
                        //3: shift+escape key no longer works
r_netgraph "0"          //Displays a graph of packet latency. A value of 2 will give additional info about what sort of data is being received from the server.
r_speeds "0"
r_fullbright "0"        //Ignore world lightmaps, drawing *everything* fully lit.
scr_centersbar "1"
scr_consize "0.833333"
scr_allowsnap "1"
scr_printspeed "8"
seta con_logcenterprint "1"     //Specifies whether to print centerprints on the console.
                                //0: never
                                //1: single-player or coop only.
                                //2: always.
scr_centertime "3"
showpause "1"
scr_turtlefps "10"
showturtle "0"
seta scr_conalpha "0.7"
scr_conspeed "1000"
set cl_cursor_bias_y "0.8"
set cl_cursor_bias_x "7.5"
seta cl_cursor_scale "1.0"
cl_cursor ""
scr_sshot_prefix "screenshots/fte-"
scr_sshot_compression "75"
seta scr_sshot_type "png"       //This specifies the default extension(and thus file format) for screenshots.
                                //Known extensions are: png, jpg/jpeg, bmp, pcx, tga, ktx, dds.
scr_fov_mode "0"        //Controls what the fov cvar actually controls:
                        //0: largest axis (ultra-wide monitors means less height will be visible).
                        //1: smallest axis (ultra-wide monitors will distort at the edges).
                        //2: horizontal axis.
                        //3: vertical axis.
                        //4: 4:3 horizontal axis, padded for wider resolutions (for a more classic fov)
r_viewmodel_fov ""      //field of vision, 1-170 degrees, standard fov is 90, nquake defaults to 108.
fov "110"               //field of vision, 1-170 degrees, standard fov is 90, nquake defaults to 108.
viewsize "100"

// Realtime Lighting
r_shadow_shadowmapping_depthbits "16"   //Shadowmap depth bits. 16, 24, or 32.
r_shadows_focus "0 0 0" //Offset for the center of the fake-shadows volume.
r_shadows_throwdirection "0 0 -1"       //The direction to throw the fake shadows in. Should ideally be opposite to r_sun_dir, but that just shows how fake these things actually are.
r_shadows_fakedistance "1024"   //The radius to use for fake shadows.
r_sun_colour "0 0 0"    //Specifies the colour of sunlight that appears in the form of crepuscular rays.
r_sun_dir "0.2 0.5 0.8" //Specifies the direction that crepusular rays appear along
r_shadow_shadowmapping_bias "0.03"
r_shadow_shadowmapping_nearclip "1"
r_shadow_shadowmapping_precision "1"    //Scales the shadowmap detail level up or down.
r_shadow_shadowmapping "1"      //Enables soft shadows instead of stencil shadows.
r_shadow_playershadows "1"      //Controls the presence of shadows on the local player.
r_shadow_realtime_world_lightmaps "0"   //Specifies how much of the map's normal lightmap to retain when using world realtime lights. 0 completely replaces lighting.
seta r_shadow_realtime_dlight_shadows "1"       //Allows dynamic realtime lights to cast shadows as they move.
r_shadow_realtime_dlight_specular "4"
r_shadow_realtime_dlight_diffuse "1"
r_shadow_realtime_dlight_ambient "0"
seta r_shadow_realtime_dlight "1"       //Enables the use of dynamic realtime lights, allowing explosions to use bumpmaps etc properly.
seta r_shadow_realtime_world_shadows "1"
seta r_shadow_realtime_world "0"        //Enables the use of static/world realtime lights.
r_shadow_scissor "1"    //constrains stencil shadows to the onscreen box that contains the maxmium extents of the light. This avoids unnecessary work.

// Hacky bug workarounds
r_ignoreentpvs "1"      //Disables pvs culling of entities that have been submitted to the renderer.
r_dodgymiptex "1"       //When enabled, this will force regeneration of mipmaps, discarding mips1-4 like glquake did. This may eg solve fullbright issues with some maps, but may reduce distant detail levels.
r_dodgypcxfiles "0"     //When enabled, this will ignore the palette stored within pcx files, for compatibility with quake2.
r_dodgytgafiles "0"     //Many old glquake engines had a buggy tga loader that ignored bottom-up flags. Naturally people worked around this and the world was plagued with buggy images. Most engines have now fixed the bug, but you can reenable it if you have bugged tga files.

// Video config
vid_desktopsettings "0" //Ignore the values of vid_width and vid_height, and just use the same settings that are used for the desktop.
vid_displayfrequency "0"
vid_height "2160"       //The screen height to attempt to use, in physical pixels. 0 means use desktop resolution.
vid_width "3840"        //The screen width to attempt to use, in physical pixels. 0 means use desktop resolution.
vid_triplebuffer "0"    //Specifies whether the hardware is forcing tripplebuffering on us, this is the number of extra page swaps required before old data has been completely overwritten.
seta vid_conautoscale "0"       //Changes the 2d scale, including hud, console, and fonts. To specify an explicit font size, divide the desired 'point' size by 8 to get the scale. High values will be clamped to maintain at least a 320*200 virtual size.
vid_conheight "360"
vid_conwidth "640"
vid_bpp "32"            //The number of colour bits to request from the renedering context
vid_fullscreen "1"
vid_renderer "gl"       //Specifies which backend is used. Values that might work are: sv (dedicated server), headless (null renderer), vk (vulkan), gl (opengl), egl (opengl es), d3d9 (direct3d 9), d3d11 (direct3d 11, with default hardware rendering), d3d11 warp (direct3d 11, with software rendering).
in_windowed_mouse "1"
vid_winthread ""        //When enabled, window messages will be handled by a separate thread. This allows the game to continue rendering when Microsoft Windows blocks while resizing, etc.
vid_wndalpha "1"        //When running windowed, specifies the window's transparency level.
vid_vsync "0"
vid_devicename ""       //Specifies which video device to try to use. If blank or invalid then one will be guessed.
gl_driver ""            //Specifies the graphics driver name to load. This is typically a filename. Blank for default.

// GL Renderer Options
r_loadsurfenvmaps "1"   //Load local reflection environment-maps, where available. These are normally defined via env_cubemap entities dotted around the place.
gl_lightmap_average "0" //Determine lightmap values based upon the center of the polygon. This will give a more buggy look, quite probably.
gl_lightmap_nearest "0" //Use nearest sampling for lightmaps. This will give a more blocky look. Meaningless when gl_lightmap_average is enabled.
gl_menutint_shader "1"  //Controls the use of GLSL to desaturate the background when drawing the menu, like quake's dos software renderer used to do before the ugly dithering of winquake.
gl_blend2d "1"
r_vertexlight "0"       //Hack loaded shaders to remove detail pass and lightmap sampling for faster rendering.
r_vertexdlights "0"     //Determine model lighting with respect to nearby dlights. Poor-man's rtlights.
r_floorcolour "64 64 128"
r_wallcolour "128 128 128"
gl_skyboxdist "0"       //The distance of the skybox. If 0, the engine will determine it based upon the far clip plane distance.
gl_screenangle "0"
r_fog_permutation "1"   //Renders fog using a material permutation. 0 plays nicer with q3 shaders, but 1 is otherwise a better choice.
r_fog_exp2 "1"          //Expresses how fog fades with distance. 0 (matching DarkPlaces's default) is typically more realistic, while 1 (matching FitzQuake and others) is more common.
gl_compress "0"         //Enable automatic texture compression even for textures which are not pre-compressed.
r_shaderblobs "0"       //If enabled, can massively accelerate vid restarts / loading (especially with the d3d renderer). Can cause issues when upgrading engine versions, so this is disabled by default.
gl_picmip_other "0"     //Effectively added to gl_picmip for the purposes of model textures.
gl_picmip_sprites "0"   //Effectively added to gl_picmip for the purposes of sprite textures.
gl_picmip_world "0"     //Effectively added to gl_picmip for the purposes of world textures.
gl_picmip2d "0"         //Reduce hud/menu texture sizes by some exponential factor.
gl_picmip "0"           //Reduce world/model texture sizes by some exponential factor.
leftisright "0"
gl_motionblurscale "1"
gl_motionblur "0"
r_noaliasshadows "0"
r_portalonly "0"        //Don't draw things which are not portals. Debug feature.
r_portaldrawplanes "0"  //Draw front and back planes in portals. Debug feature.
r_portalrecursion "1"   //The number of portals the camera is allowed to recurse through.
seta r_noframegrouplerp "0"
seta r_lerpmuzzlehack "1"
mod_halftexel "1"       //Offset texture coords by a half-texel, for compatibility with glquake and the majority of engine forks.
set mod_h2holey_bugged "1"      //Hexen2's holey-model flag uses index 0 as transparent (and additionally 255 in gl, due to a bug). GLQuake engines tend to have bugs that use ONLY index 255, resulting in a significant compatibility issue that can be resolved only with this shitty cvar hack.
dpcompat_psa_ungroup "0"
gl_lerpimages "1"       //Enables smoother resampling for images which are not power-of-two, when the drivers do not support non-power-of-two textures.
gl_lateswap "0"
gl_finish "0"
gl_nocolors "0"         //Ignores player colours and skins, reducing texture memory usage at the cost of not knowing whether you're killing your team mates.
gl_nohwblend "1"        //If 1, don't use hardware gamma ramps for transient effects that change each frame (does not affect long-term effects like holding quad or underwater tints).
seta gl_affinemodels "0"
gl_pbolightmaps "1"     //Controls whether to use PBOs for streaming lightmap updates. This prevents CPU stalls while the driver reads out the lightmap data (lightmap updates are still not free though).
gl_immutable_buffers "1"        //Controls whether to use immutable GPU memory allocations for static OpenGL vertex buffers. This potentially means less work for the drivers and thus higher framerates.
gl_immutable_textures "1"       //Controls whether to use immutable GPU memory allocations for OpenGL textures. This potentially means less work for the drivers and thus higher framerates.
vid_gl_context_noerror ""       //Disables OpenGL's error checks for a small performance speedup. May cause segfaults if stuff wasn't properly implemented/tested.
vid_gl_context_selfreset "1"    //Upon hardware failure, have the engine create a new context instead of depending on the drivers to restore everything. This can help to avoid graphics drivers randomly killing your game, and can help reduce memory requirements.
vid_gl_context_robustness "1"   //Attempt to enforce extra buffer protection in the gl driver, but can be slower with pre-gl3 hardware.
vid_gl_context_es "0"   //Requests an OpenGLES context. Be sure to set vid_gl_context_version to 2 or so.
vid_gl_context_compatibility "1"        //Requests an OpenGL context with fixed-function backwards compat.
vid_gl_context_forwardcompatible "0"    //Requests an opengl context with no depricated features enabled.
vid_gl_context_debug "0"        //Requests a debug opengl context. This provides better error oreporting.
vid_gl_context_version ""       //Specifies the version of OpenGL to try to create.
gl_workaround_ati_shadersource "1"      //Work around ATI driver bugs in the glShaderSource function. Can safely be enabled with other drivers too.
dpcompat_nopremulpics "0"       //By default FTE uses premultiplied alpha for hud/2d images, while DP does not (which results in halos with low-res content). Unfortunately DDS files would need to be recompressed, resulting in visible issues.
r_forceprogramify "0"   //Reduce the shader to a single texture, and then make stuff up about its mother. The resulting fist fight results in more colour when you shine a light upon its face.
                        //Set to 2 to ignore 'depthfunc equal' and 'tcmod scale' in order to tolerate bizzare shaders made for a bizzare engine.
                        //Because most shaders made for DP are by people who _clearly_ have no idea what the heck they're doing, you'll typically need the '2' setting.
r_polygonoffset_stencil_offset "1"
r_polygonoffset_stencil_factor "0.01"
r_polygonoffset_shadowmap_factor "0.05"
r_polygonoffset_shadowmap_offset "0"
r_polygonoffset_submodel_map "e?m? r?m? hip?m?" //List of maps on which z-fighting reduction should be used. wildcards accepted.
seta r_polygonoffset_submodel_offset "0"        //z-fighting avoidance. Pushes submodel depth values slightly towards the camera by a consistent distance.
seta r_polygonoffset_submodel_factor "0"        //z-fighting avoidance. Pushes submodel depth values slightly towards the camera depending on the slope of the surface.
r_showshaders "0"       //Debugging. Shows the name of the (worldmodel) shader being pointed to.
r_showfields "0"        //Debugging. Shows entity fields boxes (entity closest to crosshair). 1=ssqc, 2=csqc.
r_showbboxes "0"        //Debugging. Shows bounding boxes. 1=ssqc, 2=csqc. Red=solid, Green=stepping/toss/bounce, Blue=onground.
seta gl_texture_anisotropy "16" //Allows for higher quality textures on surfaces that slope away from the camera (like the floor). Set to 16 or something. Only supported with trilinear filtering.
d_lodbias "0"           //Biases choice of mipmap levels. Positive values will give more blury textures, while negative values will give crisper images (but will also give some mid-surface aliasing artifacts).
seta d_mipcap "0 1000"  //Specifies the range of mipmap levels to use.
dpcompat_smallerfonts "0"       //Mimics DP's behaviour of using a smaller font size than was actually requested.
r_font_postprocess_outline "0"  //Controls the number of pixels of dark borders to use around fonts.
r_font_linear "1"
seta gl_texturemode2d "gl_nearest"      //Specifies how 2d images are sampled. format is a 3-tupple 
seta gl_texturemode "gl_nearest_mipmap_linear"  //Specifies how world/model textures appear. Typically 3 letters eg lln.
                                                //First letter can be l(inear) or n(earest) and says how to sample from the mip (when downsampling).
                                                //The middle letter can . to disable mipmaps, or l or n to describe whether to blend between mipmaps.
                                                //The third letter says what to do when the texture is too low resolution and is thus the most noticable with low resolution textures, a n will make it look like lego, while an l will keep it smooth.
gl_maxdist "0"          //The distance of the far clip plane. If set to 0, some fancy maths will be used to place it at an infinite distance.
gl_max_size "1024"      //Specifies the maximum texture size that the engine may use. Textures larger than this will be downsized. Clamped by the value the driver supports.
r_clearcolour "0.12 0.12 0.12"
r_clear "0"
seta gl_blendsprites "1"        //Blend sprites instead of alpha testing them
gl_mindist "1"          //Distance to the near clip plane. Smaller values may damage depth precision, high values can potentialy be used to see through walls...
gl_shadeq1_name "*"     //Rename all surfaces from quake1 bsps using this pattern for the purposes of shader names.
r_lightprepass "0"      //Experimental. Attempt to use a different lighting mechanism (aka: deferred lighting). Requires vid_reload to take effect.
r_mirroralpha "1"       //Specifies how the default shader is generated for the 'window02_1' texture. Values less than 1 will turn it into a mirror.
r_norefresh "0"
vid_desktopgamma "0"    //Apply gamma ramps upon the desktop rather than the window.
seta vid_hardwaregamma "4"      //Use hardware gamma ramps. 0=ugly texture-based gamma, 1=glsl(windowed) or hardware(fullscreen), 2=always glsl, 3=always hardware gamma (disabled if hardware doesn't support), 4=scene-only gamma.
vid_preservegamma "0"   //Restore initial hardware gamma ramps when quitting.
vid_srgb "-1"           //-1: Only the framebuffer should use sRGB colourspace, textures and colours will be assumed to be linear. This has the effect of just brightening the screen according to a gamma ramp of about 2.2 (or .45 in quake's backwards gamma terms).
                        //0: Off. Colour blending will be wrong, you're likely to see obvious banding.
                        //1: Use sRGB extensions/support to ensure that the lighting aproximately matches real-world lighting (required for PBR).
                        //2: Attempt to use a linear floating-point framebuffer, which should enable monitor support for HDR.
                        //Note that driver behaviour varies by a disturbing amount, and much of the documentation conflicts with itself (the term 'linear' is awkward when the eye's perception of linear is non-linear).
vid_multisample "0"     //The number of samples to use for Multisample AntiAliasing (aka: msaa). A value of 1 explicitly disables it.
r_novis "0"

// Graphical Nicaties
mod_litsprites_force "0"        //If set to 1, sprites will be lit according to world lighting (including rtlights), like Tenebrae. Ideally use EF_ADDITIVE or EF_FULLBRIGHT to make emissive sprites instead.
mod_warnmodels "1"      //Warn if any models failed to load. Set to 0 if your mod is likely to lack optional models (like its in development).
mod_external_vis "1"    //Attempt to load .vis patches for quake maps, allowing transparent water to work properly.
r_tessellation_level "5"
gl_ati_truform_type "1"
r_tessellation "0"      //Enables+controls the use of blinn tessellation on the fallback shader for meshes, equivelent to a shader with 'program defaultskin#TESS'. This will look stupid unless the meshes were actually designed for it and have suitable vertex normals.
seta r_fog_cullentities "1"     //0: Never cull entities by fog...
                                //1: Automatically cull entities according to fog.
                                //2: Force fog culling regardless 
gl_dither "1"
gl_overbright_all "0"
seta gl_overbright "2"
seta r_deluxemapping "0"        //Enables bumpmapping based upon precomputed light directions.
                                //0=off
                                //1=use if available
                                //2=auto-generate (if possible)
gl_smoothcrosshair "1"
r_lodbias "0"           //Biases the level-of-detail on models (for those that have lod).
r_lodscale "5"          //Scales the level-of-detail reduction on models (for those that have lod).
seta r_replacemodels "md3 md2"  //A list of filename extensions to attempt to use instead of mdl.
seta r_shadows "1"      //Draw basic blob shadows underneath entities without using realtime lighting.
r_globalskin_count "10" //Specifies how many globalskins there are.
r_globalskin_first "100"        //Specifies the first .skin value that is a global skin. Entities within this range will use the shader/image called 'gfx/skinSKIN.lmp' instead of their regular skin. See also: r_globalskin_count.
r_skin_overlays "1"
seta r_fb_models "1"    //Enables the use of lumas on models. Note that if ruleset_allow_fbmodels is enabled, then all models are unconditionally fullbright in deathmatch, because cheaters would set up their models like that anyway, hurrah for beating them at their own game. QuakeWorld players suck.
r_fb_bmodels "1"        //Enables loading lumas on the map, as well as any external bsp models.
r_menutint "0.68 0.4 0.13"
seta r_lightmap "0"
seta r_drawflat "0"
r_max_gpu_bones ""      //Specifies the maximum number of bones that can be handled on the GPU. If empty, will guess.
seta gl_load24bit "1"
r_telealpha "1"
r_slimealpha ""
r_lavaalpha "1"
r_wateralpha "1"
r_fastskycolour "0"
seta r_fastsky "0"
r_skycloudalpha "1"     //Controls how opaque the front layer of legacy scrolling skies should be.
r_fastturb "0"
seta r_nolightdir "0"
seta r_nolerp "0"
r_lightmap_saturation "1"
seta r_dynamic "1"      //0: no standard dlights at all.
                        //1: coloured dlights will be used, they may show through walls. These are not realtime things.
                        //2: The dlights will be forced to monochrome (this does not affect coronas/flashblends/rtlights attached to the same light).
r_temporalscenecache "0"        //Controls whether to generate+reuse a scene cache over multiple frames. This is generated on a separate thread to avoid any associated costs. This can significantly boost framerates on complex maps, but can also stress the gpu more (performance tradeoff that varies per map). An outdated cache may be used if the cache takes too long to build (eg: lightmap animations), which could cause the odd glitch when moving fast (but retain more consistent framerates - another tradeoff).
                                //0: Tranditional quake rendering.
                                //1: Generate+Use the scene cache.
seta r_waterwarp "0"    //Enables fullscreen warp, preferably via glsl. -1 specifies to force the fov warp fallback instead which can give a smidge more performance.
r_drawviewmodelinvis "0"
r_drawviewmodel "1"
r_drawentities "1"      //Controls whether to draw entities or not.
                        //0: Draw no entities.
                        //1: Draw everything as normal.
                        //2: Draw everything but bmodels.
                        //3: Draw bmodels only.
mod_md3flags "1"        //The flags field of md3s was never officially defined. If this is set to 1, the flags will be treated identically to mdl files. Otherwise they will be ignored. Naturally, this is required to provide rotating pickups in quake.
seta r_fullbrightSkins "0.02"   //Force the use of fullbright skins on other players. No more hiding in the dark.
r_bloodstains "1"
gl_font "charset-3"     //Specifies the font file to use. a value such as FONT:ALTFONT specifies an alternative font to be used when ^^a is used.
                        //When using TTF fonts, you will likely need to scale text to at least 150% - vid_conautoscale 1.5 will do this.
                        //TTF fonts may be loaded from your windows directory. 'gl_font cour?col=1,1,1:couri?col=0,1,0' loads eg: c:\windows\fonts\cour.ttf, and uses the italic version of courier for alternative text, with specific colour tints.
con_textfont "charset-3"        //Specifies the font file to use. a value such as FONT:ALTFONT specifies an alternative font to be used when ^^a is used.
                                //When using TTF fonts, you will likely need to scale text to at least 150% - vid_conautoscale 1.5 will do this.
                                //TTF fonts may be loaded from your windows directory. 'gl_font cour?col=1,1,1:couri?col=0,1,0' loads eg: c:\windows\fonts\cour.ttf, and uses the italic version of courier for alternative text, with specific colour tints.
r_glsl_turbscale_refract "1"    //Controls the strength of the underwater ripples (used by the altwater glsl code).
r_glsl_turbscale_reflect "1"    //Controls the strength of the water reflection ripples (used by the altwater glsl code).
r_glsl_offsetmapping_reliefmapping "0"  //Changes the paralax sampling mode to be a bit nicer, but noticably more expensive at high resolutions. r_glsl_offsetmapping must be set.
r_glsl_offsetmapping_scale "0.04"
seta r_glsl_offsetmapping "0"   //Enables the use of paralax mapping, adding fake depth to textures.
r_shadow_heightscale_bumpmap "1"        //height scaler for 8bit _bump textures
r_shadow_heightscale_basetexture "0"    //scaler for generation of height maps from legacy paletted content.
r_shadow_bumpscale_bumpmap "4"  //bumpyness scaler for _bump textures
r_shadow_bumpscale_basetexture "0"      //bumpyness scaler for generation of fallback normalmap textures from models
r_stereo_method "0"     //Value 0 = Off.
                        //Value 1 = Attempt hardware acceleration. Requires vid_restart.
                        //Value 2 = red/cyan.
                        //Value 3 = red/blue.
                        //Value 4=red/green.
                        //Value 5=eye strain.
r_stereo_convergence "0"        //Nudges the angle of each eye inwards when using stereoscopic rendering.
r_stereo_separation "4" //How far apart your eyes are, in quake units. A non-zero value will enable stereoscoping rendering. You might need some of them retro 3d glasses. Hardware support is recommended, see r_stereo_context.
seta r_renderscale "1"  //Provides a way to enable subsampling or super-sampling
seta r_graphics "1"     //Turning this off will result in ascii-style rendering.
r_fxaa "0"              //Runs a post-procesing pass to strip the jaggies.
r_postprocshader ""     //Specifies a custom shader to use as a post-processing shader
r_refractreflect_scale "0.5"    //Use a different scale for refraction and reflection texturemaps. Because $reasons.
r_refract_fbo "1"       //Use an fbo for refraction. If 0, just renders as a portal and uses a copy of the current framebuffer.
gl_outline_width "2"    //The width of those outlines.
gl_outline "0"          //Draw some stylised outlines.
r_wireframe_smooth "0"
r_showtris "0"          //Developer feature where everything is drawn with wireframe over the top. Only active where cheats are permitted.
r_telestyle "1"         //See r_waterstyle, but affects only teleporters. If empty, defers to r_waterstyle.
r_slimestyle ""         //See r_waterstyle, but affects only slime. If empty, defers to r_waterstyle.
seta r_lavastyle "1"    //See r_waterstyle, but affects only lava. If empty, defers to r_waterstyle.
seta r_waterstyle "1"   //Changes how water, and teleporters are drawn. Possible values are:
                        //0: fastturb-style block colour.
                        //1: regular q1-style water.
                        //2: refraction(ripply and transparent)
                        //3: refraction with reflection at an angle
                        //4: ripplemapped without reflections (requires particle effects)
                        //5: ripples+reflections
r_fastturbcolour "0.1 0.2 0.3"  //The colour to use for water surfaces draw with r_waterstyle 0.
gl_specular_fallbackexp "1"
gl_specular_fallback "0.05"
gl_specular_power "32"
seta gl_specular "1"
gl_flashblendscale "0.35"
seta gl_flashblend "0"
r_coronas_fadedist "256"        //Coronas will fade out over this distance.
r_coronas_mindist "128" //Coronas closer than this will be invisible, preventing near clip plane issues.
r_coronas_occlusion ""  //Specifies that coronas should be occluded more carefully.
                        //0: No occlusion, at all.
                        //1: BSP occlusion only (simple tracelines).
                        //2: non-bsp occlusion also (complex tracelines).
                        //3: Depthbuffer reads (forces synchronisation).
                        //4: occlusion queries.
r_coronas_intensity "1" //Alternative intensity multiplier for coronas.
seta r_coronas "0"      //Draw coronas on realtime lights. Overrides glquake-esque flashblends.
r_stainfadeammount "1"
r_stainfadetime "1"
seta r_stains "0"
r_hdr_irisadaptation_fade_up "0.1"
r_hdr_irisadaptation_fade_down "0.5"
r_hdr_irisadaptation_maxvalue "4"
r_hdr_irisadaptation_minvalue "0.5"
r_hdr_irisadaptation_multiplier "2"
seta r_hdr_irisadaptation "0"
r_hdr_framebuffer "0"   //If enabled, the map will be rendered into a high-precision image framebuffer. This avoids issues with shaders that contribute more than 1 in any single pass (like overbrights). Can also be set to the name of an image format, to force rendering to that format first - interesting formats are L8, RGB565, B10G11R11F, and others.
r_lightmap_format ""    //Overrides the default texture format used for lightmaps. rgb9e5 is a good choice for HDR.
r_shadow_realtime_nonworld_lightmaps "1"        //Scaler for lightmaps used when not using realtime world lighting. Probably broken.
r_lightstylescale "1"
seta r_lightstylespeed "10"
r_lightstylesmooth_limit "2"
seta r_lightstylesmooth "1"
seta r_loadlit "1"      //Whether to load lit files.
                        //0: Do not load external rgb lightmap data.
                        //1: Load but don't generate.
                        //2: Generate ldr lighting (if none found).
                        //3: Generate hdr lighting (if none found).
                        //Note that regeneration of lightmap data may be unreliable if the map was made for more advanced lighting tools.
                        //Deluxemap information will also be generated, as appropriate.
r_image_downloadsizelimit "131072"      //The maximum allowed file size of images loaded from a web-based url. 0 disables completely, while empty imposes no limit.
r_imageextensions "dds ktx tga png jpg pcx"     //The list of image file extensions which might exist on disk (note that this does not list all supported formats, only the extensions that should be searched for).
r_ignoremapprefixes ""  //Ignores when textures were loaded from map-specific paths. FIXME: empty is currently interpretted as 1 because the alternative is too memory hungary with r_keepimages 1.
r_keepimages "false"    //Retain unused images in memory for slightly faster map loading. FIXME: a setting of 0 may be crashy! (empty is treated as 1 for now).
                        //0: Redundant images will be purged after each map change.
                        //1: Images will be retained until vid_reload (potentially consuming a lot of ram).
r_glsl_skybox_orientation ""    //Defines the axis around which skyboxes will rotate (the first three values). The fourth value defines the speed the skybox rotates at, in degrees per second.
r_skybox ""
r_skyfog "0.5"          //This controls an alpha-blend value for fog on skyboxes, cumulative with regular fog alpha.
seta r_softwarebanding "0"      //Utilise the Quake colormap in order to emulate 8bit software rendering. This results in banding as well as other artifacts that some believe adds character. Also forces nearest sampling on affected surfaces (palette indicies do not interpolate well).
gl_conback ""           //Specifies which conback shader/image to use. The Quake fallback is gfx/conback.lmp

// Realtime Light editing/importing
r_editlights_cursorgrid "1"     //snaps cursor to this grid size
r_editlights_cursorpushback "0" //how far to pull the cursor back toward the eye, for some reason
r_editlights_cursorpushoff "4"  //how far to push the cursor off the impacted surface
r_editlights_cursordistance "1024"      //maximum distance of cursor from eye
r_editlights "0"        //enables .rtlights file editing mode. Consider using csaddon/equivelent instead.
r_shadow_realtime_world_importlightentitiesfrommap "1"  //Controls default loading of world lightmaps.
                                                        //0: Load explicit .rtlight files only.
                                                        //1: Load explicit lights then try fallback to parsing the entities lump.
                                                        //2: Load only the entities lump.
r_editlights_import_specular "1"        //specular light scaler for imported lights
r_editlights_import_diffuse "1" //diffuse light scaler for imported lights
r_editlights_import_ambient "0" //ambient light scaler for imported lights
r_editlights_import_radius "1"  //changes size of light entities loaded from a map

// Protocol Extensions
_pext_infoblobs "0"     //RENAME ME WHEN STABLE. Enables the use of very large infokeys containing potentially invalid chars. Note that the userinfo is still limited by sv_userinfo_bytelimit and sv_userinfo_keylimit.
pext_replacementdeltas "1"      //Enables the use of alternative nack-based entity deltas
pext_predinfo "1"       //Enables some extra things to support prediction over NQ protocols.

// Terrain
mod_map_texscale "1"    //Defines the scale of texture texels. Use 1 for quake+quake2 maps, and 0.5 for quake3 maps.
mod_map_lights "0"      //Calculates lighting on brushes/patches.
mod_terrain_shadow_dist "2048"  //How far rays should be cast in order to look for occlusing geometry.
mod_terrain_shadows "0" //Cast rays to determine whether parts of the terrain should be in shadow.
mod_terrain_ambient "0.5"       //Proportion of ambient light.
mod_terrain_sundir "0.4 0.7 2"  //The direction of the sun (vector will be normalised).
mod_terrain_savever ""  //Which terrain section version to write if terrain was edited.
mod_terrain_defaulttexture ""   //Newly created terrain tiles will use this texture. This should generally be updated by the terrain editor.
mod_terrain_networked "0"       //Terrain edits are networked. Clients will download sections on demand, and servers will notify clients of changes.

// Map Cvar Options
r_subdivisions "2"
_q3bsp_bihtraces "0"    //Uses runtime-generated bih collision culling for faster traces.
q3bsp_ignorestyles "0"  //Ignores multiple lightstyles in Raven's q3bsp variant(and derivatives) for better batch/rendering performance.
q3bsp_mergelightmaps "1"        //Specifies whether to merge lightmaps into atlases in order to boost performance. Unfortunately this breaks tcgen on lightmap passes - if you care, set this to 0.
q3bsp_surf_meshcollision_force "0"      //Force mesh-based collisions on all q3bsp trisoup surfaces.
q3bsp_surf_meshcollision_flag "0x80000000"      //The surfaceparm flag(s) that enables q3bsp trisoup collision
map_autoopenportals "1" //When set to 1, force-opens all area portals. Normally these start closed and are opened by doors when they move, but this requires the gamecode to signal this.
map_noCurves "0"
map_noareas "0"

// master servers
net_q3master4 ""
net_q3master3 ""
net_q3master2 ""
net_q3master1 ""
net_q2master4 ""
net_q2master3 ""
net_q2master2 ""
net_q2master1 ""
net_master8 ""
net_master7 ""
net_master6 ""
net_master5 ""
net_master4 ""
net_master3 ""
net_master2 ""
net_master1 ""
net_qwmaster8 ""
net_qwmaster7 ""
net_qwmaster6 ""
net_qwmaster5 ""
net_qwmaster4 ""
net_qwmaster3 ""
net_qwmaster2 ""
net_qwmaster1 ""

// networking
net_enable_dtls ""      //Controls serverside dtls support.
                        //0: dtls blocked, not advertised.
                        //1: clientside choice.
                        //2: used where possible (recommended setting).
                        //3: disallow non-dtls clients (sv_port_tcp should be eg tls://[::]:27500 to also disallow unencrypted tcp connections).
sv_port_natpmp ""       //If set (typically to 5351), automatically configures your router's port forwarding. You can instead specify the full ip address of your router (192.168.1.1:5351 for example). Your router must have NAT-PMP supported and enabled.
sv_port "27500 26000"
sv_port_ipv6 ""         //Port to use for incoming ipv6 udp connections. Due to hybrid sockets this might not be needed. You can specify an ipv4 address:port for a second ipv4 port if you want.
sv_port_tcp6 ""
qtv_streamport ""       //Legacy cvar. Use sv_port_tcp instead.
sv_port_tcp ""
net_compress "0"        //Enables huffman compression of network packets.
net_mtu "1440"          //Specifies a maximum udp payload size, above which packets will be fragmented. If routers all worked properly this could be some massive value, and some massive value may work really nicely for lans. Use smaller values than the default if you're connecting through nested tunnels through routers that fail with IP fragmentation.
showdrop "0"
showpackets "0"
net_ice_exchangeprivateips ""   //Boolean. When set to 0, hides private IP addresses from your peers. Only addresses determined from the other side of your router will be shared. Setting it to 0 may be desirable but it can cause connections to fail when your router does not support hairpinning, whereas 1 fixes that at the cost of exposing private IP addresses.
cl_delay_packets "0"    //Extra latency, in milliseconds.
net_enable_rtcbroker "0"        //If 1, tcp ports will accept websocket connections from clients trying to broker direct webrtc connections. This should be low traffic, but might involve a lot of mostly-idle connections.
net_enable_websockets "1"       //If enabled, tcp ports will accept websocket game clients.
net_enable_http "0"     //If enabled, tcp ports will accept inbound http clients, potentially serving large files which could distrupt gameplay (This does not affect outgoing http(s) requests).
tls_provider "0"        //Controls which TLS provider to use.
                        //0: Auto.
                        //1: GNUTLS
                        //2: OpenSSL
                        //3: SSPI
net_enable_tls "0"      //If enabled, binary data sent to a non-tls tcp port will be interpretted as a tls handshake (enabling https or wss over the same tcp port.
net_enable_qtv "2"      //Controls whether inbound qtv requests will be honoured (both proxies and clients using qtvplay).
                        //0: Don't accept qtv connections.
                        //1: Accept connections.
                        //2: Accept qtv connections only from host-scopped addresses (read: 127.*.*.*, ::1, or unix sockets).
net_enable_qizmo "1"    //Enables serverside support for 'connect tcp://foo' or 'connect tls://foo' (with net_enable_tls), as well as qizmo's tcp connections and compatibles.
net_fakeloss "0"        //Simulates packetloss in both receiving and sending, on a scale from 0 to 1.
net_hybriddualstack "1" //Uses hybrid ipv4+ipv6 sockets where possible. Not supported on xp or below.
timeout "65"            //Connections will time out if no packets are received for this duration of time.
net_dns_ipv6 "1"        //If 0, disables dns resolution of names to ipv6 addresses (removing any associated error messages). Also hides ipv6 addresses in address:port listings.
net_dns_ipv4 "1"        //If 0, disables dns resolution of names to ipv4 addresses (removing any associated error messages). Also hides ipv4 addresses in address:port listings.
net_enabled "1"         //If 0, disables all network access, including name resolution and socket creation. Does not affect loopback/internal connections.

// Secondary view vars
v5_yaw "0"
v5_scaley "0.25"
v5_scalex "0.25"
v5_y "0"
v5_x "0.75"
v5_enabled "2"
v4_yaw "270"
v4_scaley "0.25"
v4_scalex "0.25"
v4_y "0"
v4_x "0.5"
v4_enabled "2"
v3_yaw "90"
v3_scaley "0.25"
v3_scalex "0.25"
v3_y "0"
v3_x "0.25"
v3_enabled "2"
v2_yaw "180"
v2_scaley "0.25"
v2_scalex "0.25"
v2_y "0"
v2_x "0"
v2_enabled "2"

// View variables
itburnsitburnsmakeitstop "0"    //Ouch
chase_up "32"
chase_back "125"
chase_active "0"
brightness "0"          //Brightness is how much 'white' to add to each and every pixel on the screen.
v_contrastboost "2"     //Amplifies contrast in dark areas
contrast "1.4"          //Scales colour values linearly to make your screen easier to see. Setting this to anything but 1 without hardware gamma will reduce your framerates a little.
gamma "0.5"             //Controls how bright the screen is. Setting this to anything but 1 without hardware gamma requires glsl support and can noticably harm your framerate.
r_projection "0"        //0: regular perspective.
                        //1: stereographic (aka: pannini).
                        //2: fisheye.
                        //3: panoramic.
                        //4: lambert azimuthal equal-area.
                        //5: Equirectangular
ffov ""                 //Allows you to set a specific field of view for when a custom projection is specified. If empty, will use regular fov cvar, which might get messy.
scr_autoid_enemycolour "15"     //The colour for the text on the nametags of non-team members.
scr_autoid_teamcolour "9"       //The colour for the text on the nametags of team members.
scr_autoid_weapon_mask "126"    //Mask of bits for weapon icons to actually show.
                                //+1: Shotgun.
                                //+2: Super Shotgun.
                                //+4: Nail Gun.
                                //+8: Super Nail Gun.
                                //+16: Grenade Launcher.
                                //+32: Rocket Launcher.
                                //+64: Lightning
                                //Showing only RL and GL is 96
                                //
scr_autoid_weapon "1"   //Display the player's best weapon as part of their nametag (when known).
scr_autoid_armor "1"    //Display armour as part of nametags (when known).
scr_autoid_health "1"   //Display health as part of nametags (when known).
scr_autoid_team "2"     //Display nametags above team members. 0: off. 1: display with half-alpha if occluded. 2: hide when occluded.
scr_autoid "1"          //Display nametags above all players while spectating.
skyroom ""              //Specifies the center position of the skyroom's view. Skyrooms are drawn instead of skyboxes (typically with their own skybox around them). Entities in skyrooms will be drawn only when r_ignoreentpvs is 0. Can also be set with the _skyroom worldspawn key. This is overriden by csqc's VF_SKYROOM_CAMERA.
v_deathtilt "1"         //Specifies whether to tilt the view when dead.
v_kickpitch "0"         //This controls part of the strength of viewangle changes from taking damage.
v_kickroll "0.6"        //This controls part of the strength of viewangle changes from taking damage.
v_kicktime "0"          //This controls how long viewangle changes from taking damage will last.
r_viewmodel_quake "1"   //Controls whether to use weird viewmodel movements from vanilla quake.
cl_bobmodel_speed "7"
cl_bobmodel_up "0.06"
cl_bobmodel_side "0.15"
cl_bobmodel "0"         //Controls whether the viewmodel should bob up and down as the player runs around.
cl_bobup "0.5"
cl_bobcycle "0.6"
seta cl_bob "0"         //Controls how much the camera position should bob up down as the player runs around.
cl_rollalpha "20"       //Controls the speed at which the view rolls according to sideways movement.
seta cl_rollangle "0"   //Controls the maximum view should tilt while strafing.
cl_rollspeed "200"      //Controls the speed required to reach cl_rollangle's tilt.
gl_polyblend_edgesize "128"     //This constrains colour shifts to the edge of the screen, with the value specifying the size of those borders.
seta gl_polyblend "0"   //Controls whether temporary whole-screen colour changes should be honoured or not. Change gl_cshiftpercent if you want to adjust the intensity.
                        //This does not affect v_cshift commands sent from the server.
gl_cshiftpercent "0"
cl_crossy "0"
cl_crossx "0"
crosshairalpha "1"
crosshairimage "crosshair6"     //Enables the use of an external/custom crosshair image
crosshaircorrect "0"    //Moves the crosshair around to represent the impact point of a weapon positioned below the actual view position.
crosshairsize "2"
crosshair "1"
crosshaircolor "252"
v_depthsortentities "0" //Reorder entities for transparency such that the furthest entities are drawn first, allowing nearer transparent entities to draw over the top of them.
v_viewheight "0"
v_bonusflash "1"        //Controls the strength of temporary screen flashes when picking up items (gl_polyblend must be enabled).
v_gunkick_q2 "1"        //Controls the strength of view angle changes when firing weapons (in Quake2).
seta v_gunkick "0"      //Controls the strength of view angle changes when firing weapons.
v_pentcshift "1"        //Controls the strength of pentagram-of-protection colour tints (gl_polyblend must be enabled).
v_ringcshift "1"        //Controls the strength of ring-of-invisibility colour tints (gl_polyblend must be enabled).
v_suitcshift "1"        //Controls the strength of envirosuit colour tints (gl_polyblend must be enabled).
v_quadcshift "1"        //Controls the strength of quad-damage colour tints (gl_polyblend must be enabled).
v_damagecshift ".2"     //Controls the strength of damage-taken colour tints (gl_polyblend must be enabled).
seta v_contentblend "1" //Controls the strength of underwater colour tints (gl_polyblend must be enabled).
v_ipitch_level "0.3"
v_iroll_level "0.1"
v_iyaw_level "0.3"
v_ipitch_cycle "1"
v_iroll_cycle "0.5"
v_iyaw_cycle "2"
v_idlescale "0"         //Enable swishing of the view (whether idle or otherwise). Often used for concussion effects.
v_centerspeed "0"
v_centermove "0.15"

// Internationalisation
seta lang "en_US"
com_highlightcolor "12" //ANSI colour to be used for highlighted text, used when com_parseutf8 is active.
set com_parseutf8 "0"   //Interpret console messages/playernames/etc as UTF-8. Requires special fonts. -1=iso 8859-1. 0=quakeascii(chat uses high chars). 1=utf8, revert to ascii on decode errors. 2=utf8 ignoring errors

// Copy protection
registered "1"          //Set if quake's pak1.pak is available

// Gamecode
seta r_meshpitch "-1"   //Specifies the direction of the pitch angle on mesh models formats, Quake compatibility requires -1.
seta com_nogamedirnativecode "1"        //FTE Quake blocks all downloads of files with a .dll or .so extension, however other engines (eg: ezquake and fodquake) do not - this omission can be used to trigger delayed eremote exploits in any engine (including FTE) which is later run from the same gamedir.
                                        //Quake2, Quake3(when debugging), and KTX typically run native gamecode from within gamedirs, so if you wish to run any of these games you will need to ensure this cvar is changed to 0, as well as ensure that you don't run unsafe clients.
gameversion_max ""      //gamecode version for server browsers
gameversion_min ""      //gamecode version for server browsers
gameversion ""          //gamecode version for server browsers
sys_platform "Win"

// Debugging
developer "0"           //Enables the spewing of additional developer/debugging messages. 2 will give even more spam, much of it unwanted.

// Scripting
mapname "dm3"           //Cvar that holds the short name of the current map, for scripting type stuff

// Server Info
com_protocolversion "3" //The protocol version used for dpmaster queries.
com_protocolname "FTE-Quake"    //The protocol game name used for dpmaster queries. For compatibility with DP, you can set this to 'DarkPlaces-Quake' in order to be listed in DP's master server, and to list DP servers.

// Filesystem
com_fullgamename "AfterQuake"   //The filesystem is trying to run this game
fs_cache "0"
cfg_reload_on_gamedir "1"

// Miscilaneous vars
r_tracker_frags "2"     //0: like vanilla quake
                        //1: shows only your kills/deaths
                        //2: shows all kills
                        //
route_shownodes "0"
dpcompat_skinfiles "0"  //When set, uses a nodraw shader for any unmentioned surfaces.
dpcompat_nofloodfill "0"        //Disables the q1mdl floodfill. Setting this to 1 may result in blue seams on the vanilla quake models.
temp_mod_lit2support "0"        //Set to 1 to enable lit2 support. This cvar will be removed once the format is finalised.
sv_loadentfiles_dir ""
sv_loadentfiles "1"
pm_noround "0"          //Disables player prediction snapping, in a way that cannot be reliably predicted but may be needed to avoid map bugs.
ezcompat_markup "1"     //Attempt compatibility with ezquake's text markup.0: disabled.
                        //1: Handle markup ampersand markup.
                        //2: Handle chevron markup (only in echo commands, for config compat, because its just too unreliable otherwise).
worker_sleeptime "0"    //Causes workers to sleep for a period of time after each job.
worker_flush "1"        //If set, process the entire load queue, loading stuff faster but at the risk of stalling the main thread.
worker_count ""         //Specifies the number of worker threads to utilise.

// client operation options
qtvcl_eztvextensions "0"
qtvcl_forceversion1 "0"
set ruleset_allow_fbmodels "1"  //When 1, allows all models to be displayed fullbright, completely ignoring the lightmaps. This feature exists only for parity with ezquake's defaults. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_watervis "1"      //When 0, this enforces ugly opaque water. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_shaders "1"       //When 0, this completely disables the use of external shader files, preventing custom shaders from being used for wallhacks. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_localvolume "1"   //Allows the use of the snd_playersoundvolume cvar. Muting your own sounds can make it easier to hear where your opponent is. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_sensitive_texture_replacements "1"        //Allows the replacement of certain model textures (as well as the models themselves). This prevents adding extra fullbrights to make them blatently obvious. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_modified_eyes "0" //When 0, completely hides progs/eyes.mdl if it is not strictly identical to vanilla quake. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_larger_models "1" //Enforces a maximum bounds limit on models, to prevent the use of additional spikes attached to the model from being used as a kind of wallhack. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_overlong_sounds "1"       //A setting of 0 will block the use of extra-long pickup sounds as item respawn timers. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_particle_lightning "1"    //A setting of 0 blocks using the particle system to replace lightning gun trails. This prevents making the trails thinner thus preventing them from obscuring your view of your enemies. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_packet "1"        //If 0, network packets sent via the 'packet' command will be blocked. This makes scripting timers a little harder. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_semicheats "1"    //If 0, this blocks a range of cvars that are marked as semi-cheats. Such cvars will be locked to their empty/0 value. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_frj "1"   //Specifies whether Forward-Rocket-Jump scripts are allowed in the current ruleset. If 0, limits on yaw speed will be imposed so they cannot be scripted. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
ruleset_allow_playercount "1"   //Specifies whether teamplay triggers that count nearby players are allowed in the current ruleset. You should not normally change this cvar from its permissive default, instead impose limits on yourself only through the 'ruleset' cvar.
cl_gunanglez "0"
cl_gunangley "0"
cl_gunanglex "0"
cl_gunz "0"
cl_guny "0"
cl_gunx "0"
cl_dlemptyterminate "1"
cl_download_mapsrc ""   //Specifies an http location prefix for map downloads. EG: "http://example.com/path/quakemaps/"
cl_threadedphysics "0"  //When set, client input frames are generated and sent on a worker thread
cl_countpendingpl "0"   //If set to 1, packet loss percentages will show packets still in transit as lost, even if they might still be received.
cl_loopbackprotocol "qw"        //Which protocol to use for single-player/the internal client. Should be one of: qw, qwid, nqid, nq, fitz, bjp3, dp6, dp7, auto. If 'auto', will use qw protocols for qw mods, and nq protocols for nq mods.
cl_splitscreen "0"      //Enables splitscreen support. See also: allow_splitscreen, in_rawinput*, the "p" command.
cl_pext_mask "0xffffffff"
cl_nopext "0"
cl_parsewhitetext "1"   //When parsing chat messages, enable support for messages like: red{white}red
cl_standardmsg "0"      //Disables auto colour coding in console prints.
msg_filter_frags "1"    //Prevents frag messages from appearing on the console.
msg_filter "0"          //Filter out chat messages: 0=neither. 1=broadcast chat. 2=team chat. 3=all chat.
cl_standardchat "0"     //Disables auto colour coding in chat messages.
mod_precache "1"        //Controls when models are loaded.
                        //0: Load them only when they're visible.
                        //1: Load them upfront.
                        //2: Lazily load them to shorten load times at the risk of brief stuttering during only the start of the map.
requiredownloads "1"    //0=join the game before downloads have even finished (might be laggy). 1=wait for all downloads to complete before joining.
cl_teamchatsound "misc/talk.wav"
cl_enemychatsound "misc/talk.wav"
cl_chatsound "1"
cl_nofake "2"           //value 0: permits \r chars in chat messages
                        //value 1: blocks all \r chars
                        //value 2: allows \r chars, but only from teammates
record_flush "0"        //If set, explicitly flushes demo data to disk while recording. This may be inefficient, depending on how your operating system is configured.
seta cl_demoreel "0"    //When enabled, the engine will begin playing a demo loop on startup.
cl_playerclass ""
w_switch ""
b_switch ""
noaim ""
msg "1"                 //Filter console prints/messages. Only functions on QuakeWorld servers. 0=pickup messages. 1=death messages. 2=critical messages. 3=chat.
drate "100000"          //A rough measure of the bandwidth to try to use while downloading (in bytes per second).
rate "6480"             //A rough measure of the bandwidth to try to use while playing. Too high a value may result in 'buffer bloat'.
bottomcolor "0x44ff00"
topcolor ""
team "[fte]"
model ""
skin "base"
spectator "1"
password ""
name "rk"
cl_download_packages "1"        //0=Do not download packages simply because the server is using them. 1=Download and load packages as needed (does not affect games which do not use this package). 2=Do download and install permanently (use with caution!)
cl_download_redirection "2"     //Follow download redirection to download packages instead of individual files. Also allows the server to send nearly arbitary download commands.
                                //2: allows redirection only to named packages files (and demos/*.mvd), which is a bit safer.
cl_download_csprogs "1" //Download updated client gamecode if available. Warning: If you clear this to avoid downloading vm code, you should also clear cl_download_packages.
cl_downloads "1"        //Allows you to block all automatic downloads.
cl_muzzleflash "1"
cl_lerp_players "0"     //Set this to make other players smoother, though it may increase effective latency. Affects only QuakeWorld.
cl_lerp_maxinterval "0.3"       //Maximum interval between keyframes, in seconds. Larger values can result in entities drifting very slowly when they move sporadically.
rcon_address ""
rcon_password ""
cl_crypt_rcon "1"       //Controls whether to send a hash instead of sending your rcon password as plain-text. Set to 1 for security, or 0 for backwards compatibility.
                        //Your command and any responses will still be sent as plain text.
                        //Instead, it is recommended to use rcon ONLY via dtls/tls/wss connections.
cl_timeout "60"
cl_serveraddress "78.141.238.193:27500" //The address of the last server you connected to
cl_servername "none"    //The hostname of the last server you connected to
cl_defaultport "27500"  //The default port used to connect to servers.
                        //QW: 27500, NQ: 26000, Q2: 27910, Q3: 27960.
cl_sendguid ""          //Send a randomly generated 'globally unique' id to servers, which can be used by servers for score rankings and stuff. Different servers will see different guids. Delete the 'qkey' file in order to appear as a different user.
                        //If set to 2, all servers will see the same guid. Be warned that this can show other people the guid that you're using.
cl_proxyaddr ""
cfg_save_name "fte"     //This is the config name that is saved by default when no argument is specified.
host_speeds "0"
cfg_save_buttons "0"    //If 1, saves the state of things such as +mlook or +forward to configs.
cfg_save_binds "1"      //If 1, saves all key bindings to configs.
cfg_save_aliases "1"    //If 1, saves aliases to configs. Note that aliases sent from servers are assumed to be server-specific and are never saved (and are forgotten on map changes, too).
cfg_save_infos "1"      //If 1, saves userinfo and serverinfo to configs.
seta cfg_save_auto "0"  //If 1, the config will automatically be saved and without prompts. If 0, you'll have to save your config manually (possibly via prompts from the quit menu).
cfg_save_all "1"        //If 1, cfg_save ALWAYS saves all cvars. If 0, cfg_save only ever saves archived cvars. If empty, cfg_save saves all cvars only when an explicit filename was given (ie: when not used internally via quit menu options).

// Darkplaces compatibility
dpcompat_stats "0"
dpcompat_console "0"    //Enables hacks to emulate DP's console.
dpcompat_set "0"

// Console
cl_aliasoverlap "1"
ruleset_allow_in "1"

// Teamplay
noskins "2"
baseskin "base"         //The name of the player skin to use as a fallback.
tp_disputablemacros "1"

// System vars
sys_disableTaskSwitch "0"
sys_disableWinKeys "1"
sys_clocktype "0"       //Controls which system clock to base timings from.
                        //0: auto
                        //1: timeGetTime (low precision).
                        //2: QueryPerformanceCounter (may drift, desync between cpu cores, or run fast with longer uptimes depending on cpu(s) and windows version).
sys_highpriority "0"    //Controls the process priority

// Console logging
ipautodump "1"          //Enables dumping the 'iplog.txt' file, which contains a log of usernames seen for a given IP, which is useful for detecting fake-nicks.
log_timestamps "1"
log_dosformat "1"
log_rotate_files "0"
log_rotate_size "131072"
log_developer "0"       //Enables logging of console prints when set to 1. Otherwise unimportant messages will not fill up your log files.
log_readable "7"        //Bitfield describing what to convert/strip. If 0, exact byte representation will be used.
                        //&1: Dequakify text.
                        //&2: Strip special markup.
                        //&4: Strip ansi control codes.
log_dir ""
log_name_rcon "rcon"
log_enable_rcon "1"
log_name_players "players"
log_enable_players "0"
log_name "qconsole"
log_enable "0"

// Console controls
cl_noblink "0"          //Disable the ^^b text blinking feature.
con_ocranaleds "2"
con_savehistory "1"     //Write/update conhistory.txt
con_window "1"          //States whether the console should be a floating window as in source engine games, or a top-of-the-screen-only thing.
seta con_textsize "8"   //Resize the console text to be a different height, scaled separately from the hud. The value is the height in (virtual) pixels.
con_timeformat "(%H:%M:%S) "
con_timestamps "0"
con_separatechat "0"
con_notifytime_chat "8"
con_numnotifylines_chat "8"
con_maxlines "1024"
cl_chatmode "0"         //0(nq) - everything is assumed to be a console command. prefix with 'say', or just use a messagemode bind
                        //1(q3) - everything is assumed to be chat, unless its prefixed with a /
                        //2(qw) - anything explicitly recognised as a command will be used as a command, anything unrecognised will be a chat message.
                        /// prefix is supported in all cases.
                        //ctrl held when pressing enter always makes any implicit chat into team chat instead.
con_showcompletion "1"
con_displaypossibilities "1"
seta con_notifylines "4"
con_notify_w "1"
con_notify_y "0"
con_notify_x "0"
con_notifytime "3"
con_centernotify "0"
