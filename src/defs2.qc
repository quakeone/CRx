//
// Defs2.qc                                   (Elodefs.qc)
//
// Constants and global variables used in the Elohim mod.
//
// All code by J.P. Grossman unless otherwise commented.
//
// See Elohim.qc for a list of standard fields used.
//

// team info helpers
void() team_info_reset_receiver_state;
void(entity target) team_info_mark_target_dirty;

// constants
float CLANRING_SUICIDE_OK    		= 0;
float CLANRING_TOO_MANY_SUICIDES 	= 1;
float CLANRING_NEGINF    			= -9999999;

// client styles
float CLANRING_NEW_CLIENT    	= 1;			// set when a client first connects
float CLANRING_ADMINISTRATOR    = 2;			// set if the client is an admin
float CLANRING_AFK_CLIENT    	= 4;
float CLANRING_MUTED			= 8;
float CLANRING_READY 			= 16;           // ready to play in match
float CLANRING_YES 				= 32;           // used for voting
float CLANRING_HEADS_UP 		= 64;          	// enable heads up display
float CLANRING_SETTING_OPTIONS 	= 128;  		// set when setting options
float CLANRING_MOTD 			= 256;          // showing MOTD
float CLANRING_OK_TO_SHOOT 		= 512;      	// don't fire after options/respawn
float CLANRING_KICK_CONFIRM 	= 1024;    		// used to confirm a kick
float CLANRING_CONFIRM_LEAVE 	= 2048;   		// confirmation for leaving match
float CLANRING_CONNECTED 		= 4096;       	// client has been put in server
float CLANRING_FOUND_BINDINGS 	= 8192; 		// used to check for bindings
float CLANRING_SMART_WEAPON    	= 16384;		// whether or not to use `smart' weapon switching
float CLANRING_OBSERVER    		= 32768;		// set if the client is an observer
float CLANRING_TYPING_CLIENT    = 65536;		// is the client typing
float CLANRING_SCOREBOARD 		= 131072;		// player is looking at teamscores
float CLANRING_NO_POST_THINK 	= 262144;		// don't do post think (optimization)
float CLANRING_WINQUAKE 		= 524288;     	// player is using winquake
float CLANRING_GLQUAKE 			= 1048576;     	// player is using glquake
float CLANRING_AUTOSTATS    	= 2097152;
float CLANRING_RED_TEAM			= 4194304;
float CLANRING_BLUE_TEAM		= 8388608;

// observer flags
float OBSERVER_WALK             = 1;            // set in walk mode
float OBSERVER_FLY              = 2;            // set in fly mode
float OBSERVER_CHASE            = 4;            // set in chase mode
float OBSERVER_DEMO             = 8;            // see through chasetargets eyes
float OBSERVER_CAMERA           = 16;		    // set to camera mode
float OBSERVER_AUTO             = 32;	        // view autoswitching
float OBSERVER_TOURNEY          = 64;	        // set tournement mode for chasecam
float OBSERVER_GO_AUTO          = 128;          // player entering an auto mode

// QuakeTag feature toggles (persisted via cvars)
float clanring_allow_qwsucks;  // 0 = disabled, 1 = enabled (regular Tag)
float clanring_allow_q14ever;  // 0 = disabled, 1 = enabled (special 14-mode)
float OBSERVER_COMMENTARY       = 256;          // see running commentary
float OBSERVER_POP_FORWARD    	= 512;
float OBSERVER_QUAKETV 			= 2048;		    // Sputnik: added this for streaming via quaketv

/*
// CRMOD qsmack variables
.float qsmack_level;
.float qsmack_trylevel;
float qsmack_statusokay;
*/

// server mode - stored in cvar 'samelevel' and preserved across levels
float CLANRING_NORMAL_MODE    		= 1;            //R00k:changed
float CLANRING_MATCH_MODE    		= 2;
float CLANRING_PRACTICE_MODE    	= 4;
float CLANRING_SERVER_INITIALIZED   = 8;
float CLANRING_NO_GIBS    		    = 16;
float CLANRING_LOW_GIBS    		    = 32;
float CLANRING_NO_QUAD    		    = 64;
float CLANRING_NO_PENT    		    = 128;
float CLANRING_NO_RING    		    = 256;
float CLANRING_MODE_VOTE    		= 512;
float CLANRING_CAPTURE_THE_FLAG		= 1024;
float CLANRING_NO_SPOOGE    		= 2048;
float CLANRING_RUNES			    = 4096;
float CLANRING_EXTENDED_OBITS    	= 8192;
float CLANRING_CONSOLE_INITIALIZED  = 16384;
float CLANRING_WIPEOUT_MODE			= 32768;
float CLANRING_FRESH_SPAWNS         = 0;//65536;//used?
float CLANRING_AIRSHOT_MODE			= 65536;
float CLANRING_AUTO_PAUSE    		= 131072;
float CLANRING_RUNETOSS    		    = 262144;
float CLANRING_NO_HOOK			    = 524288;
//float CLANRING_FREEZETAG			= 0;
//float CLANRING_SPEEDBALL			= 0;
//float UNUSED						= 1048576;
float CLANRING_DMM4_MODE			= 1048576;
float CLANRING_CONSOLE_STATS   		= 2097152;
float CLANRING_CA_MODE				= 4194304;
float CLANRING_RA_MODE				= 8388608;
float CLANRING_HH_MODE             = 16777216;

/*
	G A M E C O N F I G
	clanring_gameconfig - preserved across levels in cvar 'gamecfg'
	eg. (clanring_gameconfig & CLANRING_NO_HOOK)
*/
// TODO: enumflags

// float CLANRING_AUTO_PAUSE    		= 1;
// float CLANRING_CA_CC_VOTE			= 2;
float CLANRING_DISABLE_VOTE				= 4;
// float CLANRING_EXTENDED_OBITS    	= 8;
float CLANRING_ALTERNATIVE_ENTS    		= 16;
// float CLANRING_FRESH_SPAWNS         	= 32;
// float CLANRING_MODE_VOTE    			= 64;
// float CLANRING_NO_HOOK			    = 128;
// float CLANRING_NO_PENT    			= 256;
// float CLANRING_NO_QUAD    			= 512;
// float CLANRING_NO_RING    			= 1024;
// float CLANRING_NO_SPOOGE    			= 2048;
float CLANRING_POWERUP_DROP				= 4096;
// float CLANRING_RUNES			    	= 8192;
// float CLANRING_RUNETOSS    		    = 16384;
float CLANRING_SPAWNFRAG_PROTECTION 	= 32768;
float CLANRING_SEQUENTIAL_SPAWNS		= 65536;
// float CLANRING_BREAKAWAY_HOOK		= 0;
// float CLANRING_QW_ROCKET_DAMAGE		= 0;		//set rocket damage to 110 eliminate the ' + random () * 20 '
float CLANRING_DROP_DEATH_SHARDS		= 1048576;
// float CLANRING_VOTE_MAJORITY_PASS	= 0;
float CLANRING_ENABLE_MATCHSOUNDS = 2097152;
// clanring_state - not preserved across level changes
float CLANRING_TIMER_STARTED  		= 1;
float CLANRING_MATCH_STARTED    	= 2;
float CLANRING_MATCH_PAUSED    		= 4;
float CLANRING_MATCH_OVER    		= 8;
float CLANRING_MATCH_WAIT  	    	= 16;// 5 second delay after match ends
float CLANRING_KICKING    		    = 32;
float CLANRING_IGNORE_CONSOLE		= 64;
// muddle counts :)
float CLANRING_SETQUAD_COUNT    	= 128;
float CLANRING_SETPENT_COUNT    	= 1024;
float CLANRING_SETRING_COUNT    	= 8192;
// end muddle (eats bits up to 65536)
float CLANRING_NO_PRACTICE    		= 131072;
float CLANRING_TEAM_CAPTURE_CUSTOM	= 262144;
float CLANRING_UNPAUSING    		= 524288;
float CLANRING_AUTO_PAUSED    		= 1048576;
float CLANRING_DELAYED_AUTOSS    	= 2097152;
float CLANRING_TAKING_AUTOSS    	= 4194304;
float CLANRING_NULL_FLAG		    = 8388608;//R00k: added, used to make the flags untouchable during match start countdown, ie "Match begins in T minus"...

// --- Auto-ghost Quad (1v1 in normal DM / HH FFA) state ---
float g1v1_in_effect;       // 1 while the special 1v1 window is active
float g1v1_quad_ghosted;    // 1 if quad is ghosted (blocked) during 1v1
float g1v1_saved_no_quad;   // remembers (clanring_playmode & CLANRING_NO_QUAD) when entering 1v1
float g1v1_next_check_time; // next time to recheck counts in StartFrame

// teammate overlay (QSS-M/FTE //tinfo)
float TEAM_INFO_POS_GRID = 8;
float TEAM_INFO_SPEED_BAND = 10;
float TEAM_INFO_SPEED_DEADZONE = 20;
float TEAM_INFO_FULLSYNC_INTERVAL = 20;
float team_info_global_gen;
float team_info_send_force_fullsync;

// admin constants
float ADMIN_NORMAL_MODE    	= 0;
float ADMIN_PRACTICE_MODE   = 1;
float ADMIN_MATCH_MODE    	= 2;
float ADMIN_CTF_MODE    	= 3;
float ADMIN_CA_MODE    		= 4;
float ADMIN_RA_MODE    		= 5;
float ADMIN_AIRSHOT_MODE    = 6;
float ADMIN_DMM4_MODE    	= 7;
float ADMIN_WIPEOUT_MODE	= 8;
float ADMIN_HH_MODE         = 9;

// entity constants
float CLANRING_MANUAL_SPAWN = 1;

// global variables
//float   clanring_player_ids;        // used for assigning unique player ID's
float   clanring_devparam;          // used for development
float   clanring_playmode;          // current play mode (non volatile flags)
float	clanring_gameconfig;		// playmode sub-settings (non volatile flags)
float   clanring_sv_pak0_only;      // runtime snapshot of cvar sv_pak0_only
float   clanring_state;             // server state (volatile flags)
float   clanring_timelimit;         // timelimit for matches (default 20 minutes)
float   default_timelimit;          // reset timelimit between matches in case of overtime or interrupted match abort
float   clanring_overtime;          // length of auto-overtime; 0 if none
float   clanring_match_initial_timelimit; // regulation timelimit captured at match start, preserved across OT
float   clanring_overtime_count;    // number of overtime periods entered in the current match
float   clanring_nextcheck;         // used for periodic checks
float   clanring_nextcheck2;	    // used for frequent periodic checks
entity  clanring_request;           // used to control voting
float   clanring_vote_retry_time;   // next time global "vote in progress" spam message may repeat
float   clanring_level;             // number of current level in user level order
float   clanring_levelcount;        // used to scan through list in levels.cfg
float   clanring_lastcount;         // used to detect end-of-file in levels.cfg
float   clanring_force_ring;        // 1 = force ring on, -1 = force ring off, 0 = no force (auto)
float   clanring_force_timelimit;   // 1 = force timelimit from cfg, 0 = auto
float   clanring_config_batching;   // defer public config-derived updates until parsing settles
float   clanring_matchtime_sync_pending; // matchtime changed while config publication was deferred
float   clanring_ffa_start_lock_config_ready; // clanring.cfg has finalized teamplay-sensitive start-lock mode checks
float   clanring_ffa_start_lock_start_time; // server time when the current level's FFA lock began
float   clanring_ffa_start_lock_until; // server time when the FFA post-changelevel lock releases
float   clanring_ffa_start_lock_timer_last; // last start-lock second sent to the ProQuake timer clock
float   hh_maxheads = 0;            // 0 = no carry cap; positive values enforce a cap
float   hh_altar_scoring;           // allows client_frag only during HH altar scoring
float   hh_collect_taken;           // temp pickup count for HH helper transfers
float   hh_collect_reclaimed;       // temp reclaim count for HH helper transfers
float   hh_hud_enabled;             // legacy CSQC export toggle; HH now uses server-driven status

float   dm_num_spawns;              // number of deathmatch spawn points (as opposed to ctf spawn points)
float   clanring_num_starts;        // number of match extra start points
float   clanring_num_cameras;       // number of cameras
string  clanring_version;           // used for motd and help-server
string  clanring_version_info;      // text info including rev. and compile time
string  clanring_modname_info;      // human-facing mod name with dynamic version
string  clanring_version_number;    // text info storing version number (ie "4.0")
entity  clanring_debug_client;      // used for debugging
entity  clanring_quad;		        // pointer to quad for quick reference
entity  clanring_pent;		        // pointer to pent for quick reference

// match global variables
float   clanring_match_pause_time;  // records time that match was paused
float   clanring_maxteams;			// maximum number of teams
float   clanring_numteams;			// number of teams
float   clanring_maxsize;           // maximum allowed team size
entity  clanring_match_timer;       // used to time match
float   clanring_match_aborted;     // TRUE when the current match was ended early
entity  clanring_match_inactivity_check;   // scheduled inactivity watchdog during early match
float   clanring_match_start_time;  // timestamp of when early-match inactivity monitoring begins
float   CLANRING_MATCH_PLAYER_RESPAWN_DELAY = 1; // ready players respawn 1 second after match begin/end
float   CLANRING_MATCH_START_MOVEMENT_THRESHOLD = 16; // minimum distance required to count as movement
.float last_demomark_time;
.float f_trigger_block_time;  // debounce for "disabled during match" notice on f_/q_ version triggers
// --- Quad control observer announcements ---
float QUAD_RESPAWN_TIME = 60;     // seconds between quad respawns (standard QW)
float g_quad_spawner_count;       // number of Quad spawners on the map (cached)
       // used to time match
entity  clanring_sentinel;			// head of linked lists
string  clanring_szthe;			    // hack - = "The " for teamplay, "" for individual
string  clanring_szteam;			// hack - = "team" for teamplay, "player" for individual

// score global variables
string  clanring_winning_teamname;	// name of winning team (as returned by strings_get_teamname)
float   clanring_score_number;		// either difference in score or tied score
string 	CLANRING_STRING_TIED;       // initialized to "   Tied at "
float   clanring_score_count;	    // number of clients looking at score
entity 	clanring_first_place;
entity 	clanring_second_place;
float   match_last_ts1_color;       // -1=unposted, -2=teamscore1:none, >=0=teamscore1:<color>
float   match_last_ts2_color;       // -1=unposted, -2=teamscore2:none, >=0=teamscore2:<color>

// console global variables
float 	console_deathmatch;
float 	console_timelimit;
float 	console_fraglimit;
float 	console_pausable;
float 	console_noexit;

// auto parameters
float auto_parm1;	// 
float auto_parm2;	// 
float auto_parm3;	// 
float auto_parm4;	// 

// other global variables
string nextmap;                     // Non-empty values are strzone-owned; free before clearing/replacing.
float timelimit;                    // this used to be local in CheckRules in Client.qc        
float fraglimit;                    // this used to be local in CheckRules in Client.qc        
float intermission_running;         // moved here from Client.qc
float intermission_exittime;        // moved here from Client.qc
.float rounddeaths;
.float respawntime;
.float just_suicided;
.float wipeout_round_kills;
.float wipeout_regen_time;
.float wipeout_spawn_time;
.float wipeout_escape_time;
.float spawn_protect_finished;
.float teamalive;
.float teamtotal;
.float quadtime;
.float totalquad;
.float quad_majority_announced;    // per-player: have we announced >50% quad control this match?
.float matchTime;   // current match time tracker

.float tp_time;     // last time the player teleported
.float tfrag_time;  // spawn time for a tele-death trigger

entity 	clanring_best_chase_target;		// calculated every second who is the best target to chase // CRMOD - auto
entity 	clanring_current_chase_target;
float 	clanring_chase_delay;
entity 	clanring_best_camera;			// set after each camera's think
entity 	clanring_current_camera;
float 	clanring_camera_delay;

// autotrack formula selection (cr_autotrack)
//   0 = CRMod: health/armor/activity/proximity weighted score (classic)
//   1 = KTX:   item tier + frags, with ktpro event driven switching
float	CLANRING_AUTOTRACK_CRMOD	= 0;
float	CLANRING_AUTOTRACK_KTX		= 1;
float	clanring_autotrack_mode;		// which scoring formula all auto modes use
float	clanring_autotrack_event;		// an event asked for an immediate re-evaluation
entity	clanring_autotrack_hint;		// who that event wants us to look at (may be world)
float	clanring_autotrack_rl_taken;	// first rocket launcher of the match has been picked up
float	clanring_autotrack_predict_next;	// next time the quad/pent predict poll may run
float	observer_lock_enforcing;			// re-entrancy guard for the arena cam lock redirect
.float	autotrack_predict_at;			// on a powerup: the respawn we already predicted for
.entity	autotrack_team_best;			// on a team: best pov among that team's live players
.float	autotrack_team_score;			// on a team: that player's score, for the compare

vector 	clanring_angle;					// used for both autoss and intermission

// client variables
.vector clanring_qdwep;             // vector to keep track of quad weapon stats	// CRMOD - stats
.vector clanring_qdkill;            // vector to keep track of quad kill stats		// CRMOD - stats
.vector clanring_ptwep;             // vector to keep track of pent weapon stats	// CRMOD - stats
.vector clanring_ptkill;            // vector to keep track of pent kill stats		// CRMOD - stats
.vector clanring_badstats;          // keep track of RL paks and damage done to team
.vector clanring_badstats2;         // keep track of lava, axe, falling deaths
.float  clanring_badstats_bores;    // self kills with own rockets/grenades
.float  clanring_badstats_spawnwalk; // times player entered a spawn point volume
.float  clanring_badstats_spawnwalk_on; // debounce state: currently overlapping a spawn point
.float  clanring_badstats_spawnwalk_ignore_until; // suppress counting immediately after spawn/restore and briefly after a count
.float  clanring_badstats_telewalk; // times player entered a teleporter exit volume
.float  clanring_badstats_telewalk_on; // debounce state: currently overlapping a teleporter exit
.float  clanring_badstats_telewalk_ignore_until; // suppress counting immediately after spawn/restore and briefly after a count
.entity clanring_walkover_cache_next; // linked-list pointer for cached spawn/tele walkover entities
.float join_time;					// time when player joined the server
.float clanring_ghostless;             // admitted to a match without reconnect state
// Shared by ghost binding and vote-IP identity hashing. Re-rolling it mid-map
// would invalidate both saved ghost fingerprints and connected vote groups.
float clanring_ghost_key;               // per-map key for address fingerprints
float clanring_ghost_probe_fails;       // unverified restore failures inside the current window
float clanring_ghost_probe_seen;        // time of the last unverified failure
float clanring_ghost_probe_wait;        // server-wide floor delay on unverified restores
.float clanring_ghost_ip;               // ghost: address fingerprint, low word
.float clanring_ghost_ip2;              // ghost: address fingerprint, high word
.float clanring_ghost_fails;            // failed ghost code attempts this connection
.float clanring_ghost_wait;             // time before another ghost code attempt is accepted
.vector clanring_backpacks;			// track rl|lg|gl lost/taken backpacks
.vector deathorigin;
//.float killed_who[16];				// players you killed; the number of times killed_who[playerno].netname
//.float who_killed[16];				// players who killed you; how many times you died by who_killed[playerno].netname
.entity clanring_othertarget;       // entity to focus on in tourneycam		// CRMOD - tourneycam
.entity clanring_inflicted;         // stores entity last attacked			// CRMOD - combat
.float  clanring_chase_score;       // same as above, also used by cameras
.float  clanring_did_damage;        // last time person did damage
.float  oflags;				        // CRMOD observer flags
.float  observer_number;            // Target player number for eyecam
.float  clanring_cam_delay;         // keep autocam from overswitching
.vector clanring_oldangle;          // set by angles_fixangle
.float  delayed_impulse;		    // saves self.impulse so it doesn't get nuked by a ping
.float  timeslot;			        // records which scoreboard slot is used for time
.entity next_player;		        // used to point to first/next player in team
.entity next_team;			        // used to point to next team
.entity next_camera;		        // used to point to next camera
.vector clanring_old_velocity;      // used for pausing/unpausing
.float  ffa_start_locked;           // player is currently held by the FFA post-changelevel lock
.float  ffa_start_lock_nextprint;   // throttle per-client FFA lock countdown centerprints
.float  ffa_start_lock_lastsound;   // last FFA start-lock second that played the countdown beep
.float  ffa_join_brightfield_pending; // fresh connect has not yet entered an FFA game
.float  ffa_join_brightfield_until; // EF_BRIGHTFIELD is owned by the fresh-join FFA timer until this time
.entity ffa_item_ghost_ent;         // hidden timer or visual clone for FFA item alpha ghosting
.float  clanring_hack_count;	    // used to count admin hacking attempts
.float  quadDisabledMsgShown;     // 0=not shown yet this map, 1=shown (for 1v1 quad disabled message)
.float  ctf_practice_flag_msg_count; // inactive flag notices shown during this connection
.float  ctf_practice_flag_last_touch; // debounce continuous contact with an inactive flag

// Saved position support (prewar practice)
.vector saved_pos_origin;
.vector saved_pos_angles;
.vector saved_pos_velocity;
.float  saved_pos_valid;
.float  saved_pos_last_move_time;

// Early match inactivity detection
.vector match_start_origin;
.float  match_start_monitor_active;
.float  match_start_origin_valid;
.float  match_start_moved;
.float  match_start_warned;

float 	CLANRING_ANGLE_SMOOTHING    = 0.6; // CRMOD use this instead of typing the number a billion times (actually 6 times in observer.qc ;) )


//-----------------------------------------------------------------------------------------------------------------------------------------------
/*
 * helpers. -- from cspreee
 */
 #define redify(s) strconv(0,2,2,s)
 #define whitey(s) strconv(0,1,1,s)
 #define alternate(s) strconv(0,5 + ((time*4)&1), 5 + ((time*4)&1),s)
 #define flash(s) strconv(0,1 + ((time*4)&1), 1 + ((time*4)&1),s)
 #define goldnmbr(s) strconv(0,3,3,s)
 #define lowrcase(s) strconv(1,0,0,s)
 #define brackets(s) sprintf("%s%s%s", chr2str(0x90), s, chr2str(0x91))
 #define disconnected(s) sprintf("%s %s", chr2str(0x87), s)
//---------------------------------------------------------- 
void(string s1, ...) cprint;
void(string s1, string s2) cprint2;
void(string s1, string s2, string s3) cprint3;
void(string s1, string s2, string s3, string s4) cprint4;
void() votables_map_vote_prompt_display;
float(float mapnum) votables_maybe_prompt_map_vote_mode_change;
.float 	rotthink;//megahealth think

.string name_ready;
.string oldname;
.string name_before_typing; // stores previous display name while typing is active
.float  had_ready_before_typing; // 1 if a formatted status (name_ready) was shown when typing began
.string trueip;

.float 	plag;
.float  lagcheck;
.float	lagcount;

.float 	warp_episode;//chosen episode to warp
.float 	warp_map;//chosen map to warp
.float  pending_map_vote_mapnum;
.float  pending_map_vote_type;
.float  pending_map_vote_mode;
.float  pending_map_vote_timeout;
.float  pending_map_vote_print_time;

//ctf variables from ZOID's 3wave
float 	TEAM_CAPTURE_CAPTURE_BONUS    			    = 5;
float 	TEAM_CAPTURE_TEAM_BONUS    			        = 10;
float 	TEAM_CAPTURE_RECOVERY_BONUS    			    = 1;
float 	TEAM_CAPTURE_FLAG_BONUS    			        = 0;
float 	TEAM_CAPTURE_FRAG_CARRIER_BONUS    		    = 2;
float 	TEAM_CAPTURE_FLAG_RETURN_TIME    		    = 30;//was 40
float 	TEAM_CAPTURE_CARRIER_DANGER_PROTECT_BONUS 	= 2;
float 	TEAM_CAPTURE_CARRIER_PROTECT_BONUS    		= 1;
float 	TEAM_CAPTURE_FLAG_DEFENSE_BONUS    		    = 1;
float 	TEAM_CAPTURE_RETURN_FLAG_ASSIST_BONUS    	= 1;
float 	TEAM_CAPTURE_FRAG_CARRIER_ASSIST_BONUS    	= 2;
float 	TEAM_CAPTURE_TARGET_PROTECT_RADIUS    		= 550;
float 	TEAM_CAPTURE_ATTACKER_PROTECT_RADIUS    	= 550;
float 	TEAM_CAPTURE_CARRIER_DANGER_PROTECT_TIMEOUT	= 2;
float 	TEAM_CAPTURE_CARRIER_FLAG_SINCE_TIMEOUT   	= 2;
float 	TEAM_CAPTURE_FRAG_CARRIER_ASSIST_TIMEOUT   	= 6;
float 	TEAM_CAPTURE_RETURN_FLAG_ASSIST_TIMEOUT   	= 4;
float 	TEAM_CAPTURE_UPDATE_TIME  			        = 120;
float 	TEAM_UPDATE_DELAY  				            = 0.1;
//color values (ie self.team = self.height + 1)
float 	RED            		= 5;
float 	BLUE          		= 14;
float 	YELLOW            	= 13;
float 	GREEN          		= 4;

float 	FLAG_AT_BASE  		= 0;
float 	FLAG_CARRIED  		= 1;
float 	FLAG_DROPPED  		= 2;
.float 	flag_carrier;
entity 	team1_lastspawn;
entity 	team2_lastspawn;

//variables from expert ctf
.float 	last_hurt_carrier;
float  	last_flag_capture;
float  	last_capture_team;
.float 	flag_since;
.float 	last_returned_flag;
.float 	last_fragged_carrier;
.float 	lastteamupdate;
.float  leaderboardtime;

//flagstats

.float 	fdefenses;
.float 	freturns;
.float 	fpickups;
.float 	fdrops;
.float 	fcaptures;
.float 	fcdefenses;
.float 	fcassists;
.float	fcfrags;
.float	fctime;

//.float 	onteam;
.float	true_kills;
.float 	direct_hit;
.float 	dmg_taken;
.float 	dmg_given;
.float	dmg_tally;
.float 	captime;

//weapon toss times
.float toss_time;

//enhanced engine colored lighting support
float	EF_YELLOW 		    = 32;
float   EF_BLUE         	= 64;
float   EF_RED          	= 128;
float   EF_GREEN          	= 256;

float  	sys_ticrate;
//float	sv_friction;
float clanring_ctf_ticrate;
float clanring_tdm_ticrate;

.float 	hurt_together_time;
.float 	hurt_nextthink;
.float	id_time;//delay to identify teammates

.float	killed;//used to determine base spawn points
//.float	gibbed;//time to explode player
float	modelindex_head;
.float	previous_weapon;

float(float start, float end, float stepsize) rnd_number;
//.vector norg;
float 	teamscr1;
float 	teamscr2;
.float 	statstate;
.float 	laststattime;		// time of last status update
.float 	f_status_enabled;	// cached f_status preference ("off" disables flag status)
.float 	hud_last_sig_ctf;	// last emitted CTF HUD signature (skip unchanged centerprints)
.float 	hud_last_sig_observer;	// last emitted observer heads-up HUD signature
.float 	hud_obs_skip_count;	// consecutive unchanged observer HUD skips
float 	lastteamscrtime;
float 	TEAMSCRTIME    = 1;
float 	PLAYERSTATTIME    = 1.75;

entity  runespawn;
float   runespawned     = 0;
float   ITEM_RUNE1_FLAG = 1;
float   ITEM_RUNE2_FLAG = 2;
float   ITEM_RUNE3_FLAG = 4;
float   ITEM_RUNE4_FLAG = 8;
float   ITEM_RUNE_MASK  = 15;
.float  items2;
float   IT2_SIGIL1 = 32;
float	IT2_SIGIL2 = 64;
float   IT2_SIGIL3 = 128;
float   IT2_SIGIL4 = 256;
.float  rune_notice_time;
.float  player_flag;//rune value
.float  regen_time;
.float  regeneration_sound;
.float  haste_sound;
.float 	hookdmg;// damage collector for hook
float  	clanring_kick_mode;
float  	KICK_NONE  	= 0;
float  	KICK_KICK  	= 1;
float  	KICK_BAN   	= 2;
float	KICK_AFK	= 3;
float 	KICK_LAGOUT	= 4;	//R00k
float 	sys_ready;//hold on the server is loading!!
float 	debug = 0;
.float 	ctime;
//string	clanring_version_red;
.entity hookentity;
.string client;
.float  client_ver_code; // encoded as (major*10000 + minor*100 + patch)

//---custom 'server modules' off-hand hook added for pub mode. (FIXME: remove this shit)
.float 	hook_out;
.float 	hook2;
float 	HOOK_OUT 	= 1;
float 	HOOK_SWING	= 2;
//---
entity	intermission_pos;
//float	banlist;
entity	banent;
float 	hookspeedfire;
float 	hookspeedpull;
.float  delay;

.float 	alpha;//server must support this.
.float	ping_time;//client pings
.float	packet_loss;//client packet loss percentage
.float	has_userinfo; // tracks if client has provided userinfo

.float 	hack_timeout;

// JPG - added ProQuake commands
float	PQC_NOP		    =	1;
float 	PQC_NEW_TEAM	=	2;
float	PQC_ERASE_TEAM	=	3;
float 	PQC_TEAM_FRAGS	=	4;
float	PQC_MATCH_TIME	=	5;
float 	PQC_MATCH_RESET	=	6;
float 	PQC_PING_TIMES	=	7;
// JPG - end mod

float	clanring_afk_check;
float	clanring_afk_timeout;
.float 	afk_time;
.float  afk_chat_confirmed; // client has reported AFK on this level; only then can chat=0 end it
.float  observer_attack_released; // observer attack-mode toggle fires once per press, not repeatedly while held
.float 	vote;    //client flag that holds their vote
.float  map_vote_slot; // dedicated intermission ballot (1..5); do not reuse warp_map
.float  vote_ip_notice_until; // per-client duplicate-IP warning/log cooldown
.float  vote_ip_hash;
.float  vote_ip_hash2;
.float  vote_ip_address_valid;
.float  vote_ip_private;
.float  ammo_heads;
.entity hh_head_list;
.entity hh_head_next;
.entity hh_head_owner;
.entity hh_head_last_owner;
.entity hh_head_previous_owner_1;
.entity hh_head_previous_owner_2;
.entity hh_head_previous_owner_3;
.entity hh_head_previous_owner_4;
.entity hh_head_previous_owner_5;
.float  hh_head_value;
.float  hh_head_original_team;
.float  hh_head_total_value;
.entity hh_spirit_head;
.vector hh_altar_pos;
.float  cr_ra_gravity;
.float  hh_altar_count;
.vector hh_altar1;
.vector hh_altar2;
.vector hh_altar3;
.vector hh_altar4;
.float  hh_is_head;
.float  hh_on_altar;
.float  hh_haunting;
.float  hh_haunt_over;
.float  hh_can_bite;
.entity hh_eyecam_target;
.float  hh_camp_check;
.float  hh_camp_countdown;
.float  hh_help_state;
.float  hh_help_context;
.float  hh_head_pickup_block_until;
.float  hh_stats_heads_picked;
.float  hh_stats_heads_reclaimed;
.float  hh_stats_heads_scored;
.float  hh_stats_score_value;
.float  hh_stats_peak_value;
.float  hh_stats_heads_dropped;
.float  hh_stats_haunts_started;
.float  hh_stats_haunt_bites;
.float  hh_stats_haunt_kills;
.float  hh_stats_haunt_time;
.float  hh_stats_haunt_start;

.float stats_item_ssg;				//
.float stats_item_ng;				//
.float stats_item_sng;				//
.float stats_item_gl;				//
.float stats_item_rl;				//
.float stats_item_lg;				//
.float stats_item_shells;			//
.float stats_item_nails;			//
.float stats_item_rockets;			//
.float stats_item_cells;			//
.float stats_item_health;			//
.float stats_item_mega;				//
.float stats_item_biosuit;			//
.float stats_item_ring;				//
.float stats_item_redarmor;			//
.float stats_item_yellowarmor;		//
.float stats_item_greenarmor;		//
.float stats_item_backpacks;		//
.float stats_item_runes;			//
.float stats_avenge_teammate;		//
.float stats_defense_teammate;      //
.float stats_defense_dropped_flag;  //
.float stats_defense_backpack;      //
.float stats_defense_powerup;       //
.float stats_defense_weapon;        //
.float stats_defense_armor;         //
.float stats_discharge_kills;       //
.float stats_gl_frac;               //
.float stats_gl_tot;                //
.float enemy_hurt_time;				// timestamp of when you last hurt an opponent.
.float ready_check_time;			// timestamp of when player last used ready? command

float stats_timelimit;
float stats_matchtype;
entity badstats_walkover_spawn_head;
entity badstats_walkover_tele_head;
float badstats_walkover_cache_ready;

.float  csqc_enabled;

// teammate overlay delta-send cache
.float team_info_time;
.float team_info_seen_gen;
.float team_info_fullsync_time;
.float team_info_gen;
.float team_info_check_frame;
.float team_info_cache_init;
.float team_info_qx;
.float team_info_qy;
.float team_info_qz;
.float team_info_qh;
.float team_info_qa;
.float team_info_qitems;
.float team_info_qspeed;
.float team_info_qteam;
.float team_info_qheight;
.float team_info_qteam2;

typedef struct 
{
	string	name;	// filename WITHOUT '.bsp'
	string	title;	// Welcome to Quake
	string	author; // John Romero
	// string	type;	// CTF, DM, CA, RA, DMM4
	// string	image;  // 'data/mapshots/mymap.tga'
	// float	size;	// 1v1, 2v2, 4v4, 8v8
}servermap_t;

#define WARP_CATEGORY_SIZE 50	// Change the value in server.qc: clanring_set_aliases, if you exceed 50
#define WARP_MAPLIST_SIZE 30	// Above 15 and most users have to scroll.

servermap_t warpmap[WARP_CATEGORY_SIZE * WARP_MAPLIST_SIZE];

// A virtual Rocket Arena map selects one spawn group inside another map's BSP.
typedef struct
{
	string name;        // votable/rotatable virtual map name
	string bsp;         // physical map filename without .bsp
} ra_submap_t;

// A configured spawn belongs to one ra_submaps[] entry. Multiple records let
// an arena provide distinct starts without requiring an external .ent file.
typedef struct
{
	float submap_index;
	// Keep coordinates scalar: vector members in arrays of structs only retain
	// their X component on some legacy QuakeC VM/compiler combinations.
	float coord0;
	float coord1;
	float coord2;
	float yaw;
} ra_submap_spawn_t;

#define RA_SUBMAP_LIMIT 128
#define RA_SUBMAP_SPAWN_LIMIT 512
ra_submap_t ra_submaps[RA_SUBMAP_LIMIT];
ra_submap_spawn_t ra_submap_spawns[RA_SUBMAP_SPAWN_LIMIT];
float ra_submap_count;
float ra_submap_spawn_count;
string ra_active_submap_name;
string ra_active_submap_bsp;

void() ra_load_submap_config;
void() ra_submap_init_world;
void() ra_validate_submap_spawns;
void(string requested_map) clanring_changelevel;
string() clanring_current_mapname;
float(entity spot) ra_spawn_matches_submap;

.string cr_ra_submap; // optional RA spawn-group tag from a BSP or external .ent

// Alkaline map compatibility. func_wall gives liquidbrush volumes native water
// contents when the engine supports them; the other keys retain map metadata.
.string liquidbrush;
.float liquidtype;
.vector liquidcolor;
.float liquiddensity;
.float softangle; // parse-only spotlight metadata used by some Alkaline maps

string warpmenutop[WARP_CATEGORY_SIZE]; // Customizable map categories for the warp menu contents.
string vote_map_choices[6];

float map_choices[6];

string default_nextmap;

float MAX_CUSTOM_MATCH_SOUNDS = 40;	//Upped
float clanring_matchsounds_loaded;
float clanring_matchsounds_precached;
float clanring_matchsound_count;
float clanring_matchsound_rematch_count;
float clanring_matchsound_blowout_count;

string matchsound[MAX_CUSTOM_MATCH_SOUNDS];
string matchsound_rematch[MAX_CUSTOM_MATCH_SOUNDS];
string matchsound_blowout[MAX_CUSTOM_MATCH_SOUNDS];

string clanring_last_matchsound;	//zoned copy of the last match sound played, never repeated back to back

typedef struct
{
	string player;
	//string date;
	float teamcolor;
	float statvalue;
	// --- CRX v2 optional metadata (empty / -1 for legacy rows) ---
	string rec_date;        // wall-clock timestamp when record was set (ISO-like)
	float  rec_mt_remain;   // match seconds remaining when set (or -1 if N/A)
	string rec_mt_str;      // pretty "M:SS" (or "H:MM:SS") remaining (or "")
	float  rec_ping;        // ping in ms when record was set (or -1 if N/A)
}playerrecord_t;

float PLAYER_RECORDS_MAX_COUNT = 3;
playerrecord_t playerrecord[PLAYER_RECORDS_MAX_COUNT];
playerrecord_t playerrecord2[PLAYER_RECORDS_MAX_COUNT];
float new_player_record;
float new_player_record_match;
float RECORD_LOW = 0;
float RECORD_HIGH = 1;

// Time-trial top record scratch (used by ctf_trial_get_top_record)
float tt_top_time;      // seconds
string tt_top_player;   // netname

//Arena
entity boss;

.float dmg_given;
.float dmg_given_round;
.float dmg_cache;
.float ewep;
.float round;
.float max_rounds;
.float max_players;
.float start_players;
.float ca_player_flag;
.float airborn_pos;
.float killstreak;
.float combo_time;
.float combo_frags;

//PER stats
.float q1_per;
.float stats_teamkill_quad;
.float stats_powerup_denial_quad;
.float stats_quad_secure;  // times near quad when teammate picked it up
.float strategic_returns;  // Flag returns that lead to team captures
entity last_red_returner;
entity last_blue_returner;
float last_return_time;

.float fcarries_time;        // Seconds spent carrying enemy flag (non-camping)
.float flag_carry_start;     // When current carry started
.vector last_carry_pos;      // Last position while carrying (for camping detection)
.float last_pos_check;       // When we last checked position

.float busy;
.float aborted;
.float forced;
.float draw;
.float unpausing;
.float allready;
.float suddendeath;
.float timetofight;
.float maprecycle;
.float weaponlevel;
.float motd_delay;

.entity chain_gang;
.entity next_queue;
.float next_color;

.float last_lastscores_time;  // Per-client anti-spam for "lastscores" (3s throttle)
.float next_suggest_time;     // Per-client anti-spam for broadcast pickup suggestions
.float team2;
.float wins;
.float losses;
.float cc_queue;
.float cc_queue_vote;

// Last command data structures
#define MAX_CLIENT_DISCONNECT_LOG 10  // Maximum number of disconnections to track
typedef struct
{
    string player_name;
    float disconnect_time;
} disconnect_entry_t;

disconnect_entry_t disconnect_log[MAX_CLIENT_DISCONNECT_LOG];
float disconnect_log_count;

// lastscores (disk-only); declare cross-unit functions
void() match_log_score;                        // from match.qc
void(string entry) json_append_lastscores;     // from json.qc
void() utils_show_last_scores;                 // from utils.qc
string(string s) utils_rstrip;                 // from utils.qc (trim trailing spaces)

// Persistent clock helpers (defined in utils.qc / used across units)
float() utils_server_time;
void()  utils_update_server_time;

// JSON helpers used by utils_show_last_scores (from json.qc)
string(string)              json_minify;
string(string, float)       json_array_next_object;
float                       json__it_next;  // iterator's next position
string(string, string, string) json_obj_get_string;
float(string, string, float)   json_obj_get_float;
// Persistent time tracking system (similar to hunk storage)
float server_persistent_time;     // Stores the accumulated server time
float last_time_update;           // Tracks when persistent time was last updated

entity team_first_place;
entity team_second_place;
entity team_third_place;
entity ca_mvp_entity;

float clanring_rulescheck;
.float max_teams;
float ra_wins;
entity ra_lastspawn;
//float tele_num_spawns;
.float nextspawn;

// Cross-unit helpers
float() match_aborted_too_early;    // from match.qc

float COLOR_BLUE = 13;
float COLOR_RED = 4;
float COLOR_YELLOW = 12;
float COLOR_GREEN = 3;

float TEAM_SPECTATOR 	= 0;
float TEAM_PRACTICE	= 16;
float TEAM_BLUE = 14;
float TEAM_RED = 5;
float TEAM_YELLOW = 13;
float TEAM_GREEN = 4;

float PF_WAITING = 1;
float PF_DEAD = 2;
float PF_READY = 3;
float PF_PLAYING = 4;

float CA_MATCH_WAITING = 1;
float CA_MATCH_ROUNDSTART = 2;
float CA_MATCH_PLAYING = 3;
float CA_MATCH_OVER = 4;
float CA_MATCH_CLEANUP = 5;
.float ca_deathcamtime;
.float arena_packs;
.float color2; //since we already have .vector color
.float pauseblink; // next time to blink PAUSED centerprint without corrupting boss.lip

// worldtype values
float	WORLDTYPE_MEDIEVAL		= 0;
float	WORLDTYPE_METAL			= 1;
float	WORLDTYPE_BASE			= 2;

// spawnflags for func_movewall
float	MOVEWALL_VISIBLE		= 1;
float	MOVEWALL_TOUCH			= 2;
float	MOVEWALL_NONBLOCKING	= 4;

.string		origmodel; // addition by bmFbr for switchable brushes

//
// support for item_key_custom -- iw
//
.string keyname;
.float customkeys;

.string target2;		// second target's name
.string target3;		// third target's name
.string target4;		// fourth target's name
.string targetname2;	// second name
.string targetname3;	// third name
.string targetname4;	// fourth name
.string killtarget2;	// second target to kill
string lastnameused;	// the targetname that was last used to trigger somthing

//
// switchable shadow
//
.float		switchshadstyle;
.float		shadowoff;
.entity shadowcontroller;
.float speed2;

.float is_waiting;	// Supa, triggers, wait until activated before we can trigger?

.string		mdl_debris; //dumptruck_ds

.float		onladder;

.float style2; //c0burn's switchable lights

string(float num) lightstyle_fade_lookup; //addition by bmFbr for switchable shadows
void() misc_shadowcontroller;
void() misc_shadowcontroller_use;
void() shadow_fade_in;
void() shadow_fade_out;
void() match_clear_leavemealone;

// from Custents
// variable for healing trigger
.float	heal_timer;
.float	heal_amount;
.float	health_max;
// Constants for the healing trigger
float HEAL_START_ON	= 1;
float HEAL_PLAYER_ONLY	= 2;
float HEAL_MONSTER_ONLY	= 4;

// Custom Monster Sounds START -- dumptruck_ds

.string snd_death;
.string snd_pain;
.string snd_sight;
.string snd_attack;
.string snd_hit;
.string snd_idle;
.string snd_land;
.string snd_move;
.string snd_misc;
.string snd_misc1;
.string snd_misc2;
.string snd_misc3;

// Custom Monster Sounds END -- dumptruck_ds

// Custom Model Start -- dumptruck_ds

.string mdl_head;
.string mdl_body;
.string mdl_proj;
.string mdl_exproj;
.float skin_head;
.float skin_proj;
.float skin_exproj;
.string mdl_gib1;
.string mdl_gib2;
.string mdl_gib3;

.vector mdlsz, centeroffset; // addition by bmFbr for custom model bounding box definition

// Custom Model End -- dumptruck_ds

.float drop_item; // key DropStuff

// entity state
.float estate;
.void() dormant_use;
float STATE_ACTIVE      = 0;
float STATE_INACTIVE    = 1;
float STATE_INVISIBLE = 8;

// worldspawn default mdls
.string h_vial_mdl;
.string a_shr_mdl;

float isFTEserver; //Is the server running on FTE or QSS/M?
string clanring_new_record_player;
float ra_initial_rules_shown; // RA: track if round-1 rules were already shown

// --- CRX v2: metadata scratch + read-ahead cache for stats reader ---
string _crx_peek_line;
string crx_meta_date;
float  crx_meta_mt_remain;
string crx_meta_mt_str;
float  crx_meta_ping;

// --- leave-me-alone (prewar) ---
.float leavemealone;       // 0/1 flag
.float next_lma_toggle;    // cooldown timestamp

// --- vote menu cooldown ---
.float next_vote_time;     // cooldown for vote menu changes

// Utility: fast proximity check to avoid re-solidifying inside others
float(entity center, float rad) AnySolidPlayerInRadius =
{
    local entity e;
    e = findradius(center.origin, rad);
    while (e)
    {
        if (e != center && e.classname == "player" && e.solid == SOLID_SLIDEBOX)
            return TRUE;
        e = e.chain;
    }
    return FALSE;
};

.entity enemy_target;
#define INSTALLED_NEXTMAPS_LIMIT 200
string levels[INSTALLED_NEXTMAPS_LIMIT];//levels.cfg
//eof

entity red_flag_ghost;
entity blue_flag_ghost;
void(entity baseflag) CTF_SpawnGhostFlag;
void(entity ghost) CTF_RemoveGhostFlag;
void() CTF_GhostFlagTouch;
.void(entity) webhook_prepfn;   // single-slot; PREP reads ev.message
.string hook_url;                   // prepared URL
.string hook_url2;                  // optional second URL (stats fan-out)
.string hook_url3;                  // optional third URL (stats fan-out)
.string hook_payload;               // prepared JSON payload

// Google Sheets integration forward declarations
float() gsheets_enabled;
float() gsheets_configured;
float() gsheets_read_started_current_map;
float() gsheets_read_inflight_current_map;
float() gsheets_trial_records_ready_current_map;
float() gsheets_match_records_ready_current_map;
string() gsheets_captimes_source_title;
void() gsheets_validate;
void() gsheets_read_capture_stats;
float(string lookup_map, entity requester) gsheets_read_capture_stats_lookup;
void(float is_match, float rank, string player, float teamcolor, float captime, string rec_date, string mt_str, float ping) gsheets_write_record;
void(float reqid, float status, string body) gsheets_callback;
string(string json, string key) gsheets_json_get_string;
float(string json, string key) gsheets_json_get_number;
void(string arr, float recordset) gsheets_parse_record_array;
string (string stats_filename, float include_mt, string empty_msg) captimes_read_top3_from_file;
